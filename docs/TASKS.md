# Задачи

Скрам-доска проекта: очередь → спринт → в работе → выполнено. Когда
задача решена, итоговое решение уходит в чистовик (`CONCEPT.md`/
`STACK.md`), а здесь остаётся короткая запись со ссылкой — этот файл про
статус, не про содержание решений.

## Воркфлоу спринтов

Спринт — не отрезок календарного времени, а пронумерованная пачка задач.
Система дат (дд.мм–дд.мм) упразднена 17.07.2026: реальный темп (шесть
«недельных» вех дорожной карты закрыты за два дня) сделал привязку к
неделям фиктивной — доска врала о темпе, а не описывала его. Соло-проект,
поэтому без лишних ролей и церемоний — просто явные границы «что делаем
сейчас».

1. **Бэклог** — раздел «Очередь» ниже. Весь пул задач без привязки к
   времени, не отсортирован по приоритету жёстко.
2. **Планирование** — при открытии нового спринта открываем подраздел в
   «Текущий спринт»: `### Спринт N: цель`. Цель — одна фраза, что должно
   быть верно к завершению спринта. Под ней — список задач, перенесённых
   из «Очередь» (столько, сколько реально сделать за один присест, не
   больше).
3. **В работе** — задача, которую делаем прямо сейчас. Не больше
   1-2 одновременно, чтобы не размазываться.
4. **Сверка** — короткий проход по доске перед началом новой пачки работы
   (не встреча, соло-проект):
   - что сделано → отметить/перенести в «Выполнено»;
   - что берём в работу дальше → обновить «В работе»;
   - что новое всплыло (в диалоге, в коде, где угодно) → сразу
     завести задачей в «Очередь», а если это горит и укладывается в
     цель спринта — сразу в список текущего спринта.
   Смысл сверки — не дать новым задачам потеряться и не копить их «в
   уме»: всё, что всплыло, сразу попадает в файл.
5. **Обзор и ретро** — в конце спринта:
   - сделанное → в «Выполнено» со ссылкой на раздел чистовика;
   - несделанное → обратно в «Очередь» (или явно в следующий спринт,
     если это по-прежнему приоритет);
   - при необходимости — короткая заметка в самом подразделе спринта:
     что мешало, что стоит изменить в следующем.
6. Подраздел закрытого спринта остаётся в файле как история (не
   удаляется), открывается следующий.

## Текущий спринт

### Спринт 1: рабочий монорепо-скелет (веха 1 дорожной карты)

Задачи идут в порядке зависимости — каждая следующая опирается на
предыдущую, порядок не переставлять без причины.

- [x] ~~Минимальный `.gitignore` (node_modules, dist, .env и т.д.)~~ — уже
  был в репозитории, покрывает node_modules/dist/.env.
- [x] ~~Корень монорепо: pnpm workspaces (`package.json` +
  `pnpm-workspace.yaml`)~~ — сделано, `pnpm@11.13.1` через corepack.
- [x] ~~Структура пакетов `apps/web`, `apps/api`, `packages/shared`
  (директории + минимальный `package.json` в каждом)~~ — сделано.
- [x] ~~TypeScript-конфиги — базовый `tsconfig.json` в корне,
  per-package конфиги через `extends`~~ — сделано, `pnpm -r typecheck`
  зелёный.
- [x] ~~`.env.example` + схема обязательных переменных окружения (БД,
  JWT-секрет, ключи S3 и т.д.)~~ — сделано.
- [x] ~~Удалить каталоги `backend/`, `database/`, `frontend/` (`.gitkeep`)~~ —
  сделано.

Веха 1 дорожной карты закрыта — все задачи Спринта 1 выполнены.

### Спринт 2: докер и локальное окружение (веха 2 дорожной карты)

Задачи идут в порядке зависимости.

- [x] ~~`docker-compose.yml`: сервис `postgres` + тестовая БД/схема (см.
  `STACK.md#тестирование`)~~ — сделано, `postgres:16-alpine` +
  init-скрипт `docker/postgres-init/001-create-test-db.sql` создаёт
  `grammashop_test` рядом с `grammashop`.
- [x] ~~Multi-stage `Dockerfile` для `apps/api`~~ — сделано
  (`apps/api/Dockerfile`: deps → build (`tsc`) → runtime). Плейсхолдер
  `src/index.ts` — голый `node:http`-сервер, не Fastify: настоящий
  бэкенд-скелет и его TDD — веха 4, здесь только докер-обвязка.
- [x] ~~Multi-stage `Dockerfile` для `apps/web`~~ — сделано
  (`apps/web/Dockerfile`: build → nginx runtime). Плейсхолдер —
  статический `apps/web/public/index.html`, не Vite/React: веха 5.
- [x] ~~Сервисы `api`/`web` в `docker-compose.yml`, подключены к
  `postgres`~~ — сделано, `api` ждёт `postgres` healthy.
- [x] ~~`docker compose up` поднимает всё локально, `api` и `web` отдают
  "hello world"~~ — проверено руками: `api` → `{"message":"hello
  world"}` на :3000, `web` → HTML "hello world" на :5173 (nginx).

Веха 2 дорожной карты закрыта — все задачи Спринта 2 выполнены.

### Спринт 3: база данных — Drizzle + Postgres (веха 3 дорожной карты)

Задачи идут в порядке зависимости.

- [x] ~~Drizzle ORM + drizzle-kit в `apps/api`: зависимости,
  `drizzle.config.ts`, клиент подключения к Postgres через `DATABASE_URL`
  (уже в `.env.example`)~~ — сделано (`apps/api/src/db/client.ts`).
- [x] ~~Базовая схема-заглушка (одна таблица-плейсхолдер)~~ — сделано
  (`apps/api/src/db/schema.ts`, таблица `health_check`); настоящая
  доменная схема — веха 4 вместе со скелетом Fastify по TDD.
- [x] ~~Первая миграция (`drizzle-kit generate` + применение)~~ — сделано,
  `apps/api/drizzle/0000_majestic_captain_britain.sql`, прогнана руками
  и на `grammashop`, и на `grammashop_test`.
- [x] ~~Проверка миграций в докере~~ — сделано: `apps/api/src/db/migrate.ts`
  запускается в `CMD` контейнера `api` перед стартом сервера (см.
  `apps/api/Dockerfile`), `DATABASE_URL` прокинут в `docker-compose.yml`.
  Проверено руками с нуля: `docker compose down -v` → `docker compose up`
  → лог `migrations applied` → `\dt` в `grammashop` показывает
  `health_check` → `curl localhost:3000` отдаёт hello world.

Веха 3 дорожной карты закрыта — все задачи Спринта 3 выполнены.

### Спринт 4: бэкенд-скелет Fastify (веха 4 дорожной карты)

Задачи идут в порядке зависимости. TDD (см. `STACK.md#методология---tdd`):
сначала падающий тест через `fastify.inject()`, потом код.

- [x] ~~Зависимости в `apps/api`: `fastify` (runtime), `vitest` + `tsx`
  (dev); `dev`/`test`-скрипты вместо TODO-заглушек~~ — сделано
  (`apps/api/package.json`); `dev` — `tsx watch`, `test` — `vitest run`.
- [x] ~~Zod-схема ответа health-check в `packages/shared`, шарится между
  route и (в будущем) фронтендом~~ — сделано
  (`packages/shared/src/schemas/health.ts`, `healthResponseSchema`).
  Попутно вскрылось: `packages/shared` был без реальной сборки (`main`
  смотрел на `src/*.ts` напрямую — рантайм-`node` не умеет исполнять
  TypeScript). Добавлена `tsc`-сборка (`dist`), `pnpm -r` собирает
  воркспейсы в топологическом порядке сам — см.
  [STACK.md#монорепо](STACK.md#монорепо).
- [x] ~~RED: падающий интеграционный тест `GET /health` через
  `fastify.inject()` — против реальной тестовой БД (`grammashop_test`),
  без мока Drizzle~~ — сделано (`apps/api/src/routes/health.route.test.ts`),
  тест сначала упал на отсутствующем `app.ts`, затем (после реализации) —
  на некорректной загрузке `.env` (см. ниже).
- [x] ~~GREEN: `routes → services → db` для `/health` (сервис пишет в
  плейсхолдер-таблицу `health_check`), `app.ts` (`buildApp()`), `index.ts`
  переключён на Fastify вместо заглушки на `node:http`~~ — сделано
  (`apps/api/src/app.ts`, `routes/health.route.ts`,
  `services/health.service.ts`). Попутно исправлена скрытая проблема:
  `dotenv/config` (и в новом `index.ts`, и в уже существовавшем
  `db/migrate.ts`) искал `.env` от `cwd`, а не от корня репо — молча не
  находил файл при запуске через `pnpm --filter`. Вынесено в общий
  `apps/api/src/env.ts` (резолвится от расположения файла, как уже было
  сделано для `migrationsFolder`).
- [x] ~~Проверка в докере с нуля: `docker compose up`, `curl /health`
  отдаёт `{"status":"ok"}`~~ — сделано, дважды: `docker compose down -v` →
  `up --build` → `curl localhost:3000/health` → `{"status":"ok"}`,
  `health_check` пишется. Заодно всплыло, что `grammashop_test` мигрировалась
  только вручную (эта ручная привычка тянулась из Спринта 3) — теперь
  `pnpm test` мигрирует её сам через vitest `globalSetup`
  (`apps/api/src/test/global-setup.ts`), иначе тесты ломались после
  каждого `down -v`. См. [STACK.md#тестирование](STACK.md#тестирование).

Веха 4 дорожной карты закрыта — все задачи Спринта 4 выполнены.

### Спринт 5: UX/UI-скелет React + Vite (веха 5 дорожной карты)

Задачи идут в порядке зависимости. Первый компонент — по TDD (см.
`STACK.md#методология---tdd`): сначала падающий тест через
`@testing-library/react`, потом код.

- [x] ~~Vite + React + TypeScript в `apps/web`: зависимости, `vite.config.ts`,
  реальные `dev`/`build`-скрипты вместо TODO-заглушек, `src/main.tsx`
  монтирует React-дерево, `index.html` — Vite-шаблон вместо
  `public/index.html`-заглушки~~ — сделано.
- [x] ~~React Router: три группы маршрутов (витрина / продавцовская админка /
  платформенная админка) — плейсхолдер-страница на каждую, см.
  `STACK.md#роутинг`~~ — сделано (`src/router.tsx`, `src/routes/*`);
  ограничение доступа по роли из сессии — отдельная задача авторизации на
  фронте, не в скоупе этого скелета.
- [x] ~~`shadcn/ui` подключён (Tailwind + Radix), базовая конфигурация, один
  компонент кита используется на плейсхолдер-странице, см.
  `STACK.md#ui-кит`~~ — сделано (Tailwind v4 через `@tailwindcss/vite`,
  `components.json`, `Button` на витрине).
- [x] ~~axios-клиент в `apps/web`, настроен на обращение к `apps/api`
  (`baseURL` из env), см. `STACK.md#http-клиент`~~ — сделано
  (`src/lib/api-client.ts`, `VITE_API_URL` в `.env.example`).
- [x] ~~RED: падающий тест компонента через `@testing-library/react` +
  Vitest — компонент отображает статус `/health`, полученный через
  axios-клиент~~ — сделано (`HealthStatus.test.tsx`, упал на отсутствующем
  `HealthStatus.tsx`).
- [x] ~~GREEN: компонент реализован, тест зелёный~~ — сделано
  (`src/components/HealthStatus.tsx`).
- [x] ~~Проверка в докере с нуля: `apps/web/Dockerfile` собирает реальный
  Vite-билд (не статический `public/index.html`), `docker compose up` —
  `web` отдаёт React-приложение~~ — проверено руками через `docker compose
  down -v` → `up --build` → Playwright-браузер против `localhost:5173`:
  `/` показывает "Витрина" + shadcn-кнопку + статус `/health` = "ok" (живой
  запрос к `apps/api` через axios, без моков), `/seller` и `/platform`
  отдают свои плейсхолдеры. Побочная находка: у nginx не было SPA-fallback
  (`try_files` на `index.html`) — прямые заходы на `/seller`/`/platform`
  падали 404, пофикшено `apps/web/nginx.conf` + прописано в Dockerfile.
  Заодно пришлось добавить `@fastify/cors` в `apps/api`
  (`app.ts`) — без него браузер с `:5173` не мог достучаться до `:3000`.

Веха 5 дорожной карты закрыта — все задачи Спринта 5 выполнены.

### Спринт 6: CI/CD (веха 6 дорожной карты)

Задачи идут в порядке зависимости. Реестр — GitHub Container Registry
(ghcr.io), SSH-деплой на VM сознательно вынесен из скоупа спринта — VM
(веха 7) ещё не существует, см. [STACK.md#ci-cd](STACK.md#ci-cd).

- [x] ~~GitHub Actions workflow: job тестов — `postgres` как
  service-контейнер (аналог `docker-compose`), миграции, `pnpm -r test`
  (web + api), триггер на push/PR в `main`~~ — сделано
  (`.github/workflows/ci.yml`, job `test`).
- [x] ~~Job сборки Docker-образов `apps/api` и `apps/web` (существующие
  multi-stage `Dockerfile`), зависит от зелёных тестов~~ — сделано (job
  `build-and-push`, `needs: test`).
- [x] ~~Пуш собранных образов в ghcr.io (тег по SHA коммита), авторизация
  через встроенный `GITHUB_TOKEN`~~ — сделано.
- [x] ~~Проверка руками: пуш в `main` → workflow зелёный → образы видны в
  ghcr.io пакетах репозитория~~ — проверено: run
  [29542419280](https://github.com/seshmonke/grammashop/actions/runs/29542419280)
  зелёный (оба job'а), `ghcr.io/seshmonke/grammashop-api` и
  `-grammashop-web` содержат теги по SHA коммита и `latest` (проверено
  через анонимный pull-токен ghcr.io, `/v2/.../tags/list`).

### Спринт 7: концептуальный долг + старт инфры вехи 7

Приоритет — концептуальные задачи из ревью 16.07.2026, которые дольше
всего откладывались (см. [reviews/2026-07-16.md](reviews/2026-07-16.md),
пп. 6-7), плюс первый
шаг вехи 7, чтобы не ждать полного её закрытия для начала работы с
реальным сервером.

- [x] ~~Короткий обзор конкурентов (3-5 конструкторов Telegram-магазинов в
  РФ: цены, модель оплаты, есть ли у них то же трение с ЮKassa) + абзац
  дифференциации в `CONCEPT.md`~~ — сделано, разобрано 5 сервисов
  (Botobot, BotMag, Leadteh, SaleBot, TGShop), затем механика оплаты и
  выдачи ссылки на магазин проверена глубже (не только по маркетинговым
  страницам). Два вывода: (1) оплата покупателем у всего рынка — тот же
  принцип «продавец приносит свои платёжные ключи», что и у grammashop
  на Тарифах 2-3, не более низкий порог входа, как показалось при первом
  обзоре; порог по оплате у grammashop выше рынка только для Тарифа 1
  (мануальный перевод вообще без шлюза, у конкурентов же уже на первом
  платном тарифе есть автоматизированная оплата после онбординга
  продавца); (2) у всех проверенных конкурентов продавец обязан завести
  собственного Telegram-бота через BotFather — grammashop убирает этот
  шаг целиком (общий Mini App, `seller_id` в `startapp`), это
  подтверждённый, а не гипотетический дифференциатор →
  [CONCEPT.md#конкуренты-и-дифференциация](CONCEPT.md#конкуренты-и-дифференциация).
- [x] ~~Зафиксировать в `CONCEPT.md` риск платформенной зависимости от
  Telegram~~ — сделано →
  [CONCEPT.md#риск-зависимости-от-telegram](CONCEPT.md#риск-зависимости-от-telegram).
- [x] ~~Сформировать продуктовый бэклог v1~~ — сделано, 8 пунктов от
  доменной схемы до платформенной админки → [«Дорожная карта продукта
  (v1)»](#дорожная-карта-продукта-v1) ниже.
- [x] ~~Арендовать VM под веху 7 (Yandex Cloud) — начать с минимальной
  бюджетной конфигурации для отладки деплоя (`docker-compose` + Caddy,
  см. STACK.md#хостинг-и-деплой), апгрейд по факту нагрузки, не
  заранее~~ — сделано 17.07.2026: VM арендован (`ru-central1-b`, 2 vCPU
  20%, 2 ГБ RAM, 20 ГБ SSD, статический IP), домен `grammashop.online`
  (не `.ru` — см. STACK.md#хостинг-и-деплой, идентификация через
  Госуслуги с 01.09.2026), Docker установлен. Вместо того чтобы
  остановиться на аренде (как планировал Спринт 7 изначально — «первый
  шаг вехи 7»), сразу доведено до полного рабочего деплоя: `Caddyfile` +
  прод-профиль `docker-compose.prod.yml` (TLS от Let's Encrypt проверен
  на реальном домене), SSH-деплой в CI (`.github/workflows/ci.yml`,
  джоба `deploy`) — проверено сквозным автопушем в `main`
  → [STACK.md#хостинг-и-деплой](STACK.md#хостинг-и-деплой),
  [STACK.md#ci-cd](STACK.md#ci-cd).

Задачи Спринта 7 выполнены. Веха 7 продвинута существенно дальше
изначального плана спринта, но **не закрыта целиком** — по своему
определению в дорожной карте она требует ещё S3/Sentry на бою и
настроенных бэкапов (см. новые пункты в «Очереди» ниже).

### Спринт 8: закрыть веху 7 целиком (S3, Sentry, бэкапы на бою)

Цель: веха 7 закрыта по своему определению — S3 и Sentry работают на
бою, ночной бэкап уходит в S3, восстановление проверяется автоматически.
Задачи идут в порядке зависимости (бэкапы требуют готового S3).

**Спринт закрыт 17.07.2026** — все 4 задачи сделаны, веха 7 дорожной
карты закрыта целиком.

- [x] ~~S3-бакет (Yandex Object Storage, см.
  [STACK.md#хранилище-файлов](STACK.md#хранилище-файлов)): создать бакет
  и сервисный аккаунт с ключами, ключи в `.env` на VM, проверить
  запись/чтение с боевого `api` через `@aws-sdk/client-s3`~~ — сделано
  17.07.2026: бакет `grammashop-storage` (Yandex Object Storage,
  `ru-central1`, доступ только с авторизацией, не публичный), сервисный
  аккаунт `grammashop-s3` с ролью `storage.editor` (не шире — доступ
  только к Object Storage этого каталога), статический ключ в `.env` на
  VM. Код: `apps/api/src/s3/client.ts` (`S3Client` с `forcePathStyle`),
  `apps/api/src/s3/verify.ts` (put/get/delete тестового объекта) —
  прогнан и локально, и на бою через `docker compose exec api`, оба раза
  `s3 verify ok`. `S3_*` переменные проброшены в контейнер `api` через
  `docker-compose.yml` (раньше туда попадали только `DATABASE_URL`/
  `API_PORT`, остальные переменные из `.env` не доходили до контейнера
  вообще).
- [x] ~~Sentry (free tier, см.
  [STACK.md#логирование-и-мониторинг](STACK.md#логирование-и-мониторинг)):
  подключить на `apps/api` и `apps/web`, DSN в env, проверить на бою
  тестовым исключением с обоих концов. Сразу настроить scrubbing: тела
  запросов и ПДн покупателей (ФИО/телефон/адрес из чекаута) не должны
  уходить в события Sentry — его серверы за пределами РФ, отправка ПДн
  туда = трансграничная передача по 152-ФЗ, которая нам не нужна~~ —
  сделано 17.07.2026: организация `grammashop.sentry.io` (EU data
  storage), два проекта — `grammashop-api` (Node/Fastify),
  `grammashop-web` (React). Scrubbing — два независимых слоя, не один:
  (1) `sendDefaultPii: false` — SDK не собирает тело запроса/куки/IP по
  умолчанию, это основная гарантия, детерминированная; (2) `beforeSend`
  редактирует известные ПДн-поля (фио/телефон/адрес/email) в
  extra/contexts — подстраховка сверху, не гарантия (не ловит ПДн,
  случайно попавший в текст исключения). Код:
  `apps/api/src/sentry.ts` + `setupFastifyErrorHandler` в `app.ts`,
  `apps/web/src/sentry.ts`, инициализация в `main.tsx`. DSN апи — рантайм
  через `.env` на VM (`SENTRY_DSN_API`, проброшен в контейнер `api` через
  `docker-compose.yml`, та же схема, что и у S3-переменных выше); DSN
  веба — build-time через Vite (`VITE_SENTRY_DSN_WEB`, GitHub secret
  `SENTRY_DSN_WEB`, build-arg в `apps/web/Dockerfile`/`ci.yml`, т.к.
  Vite подставляет переменные в бандл на этапе сборки, не в рантайме).
  Проверено на бою временными debug-роутами (убраны после проверки,
  следующий коммит): оба конца поймали тестовое исключение
  (`GRAMMASHOP-API-1`, `GRAMMASHOP-WEB-1` в дашборде), тел запроса/ПДн в
  событиях нет.
- [x] ~~Nightly `pg_dump` → S3 (cron/systemd timer на VM, папка
  `backups/`, ретеншн 7 дневных + 4 недельных, см.
  [STACK.md#бэкапы](STACK.md#бэкапы))~~ — сделано 17.07.2026: скрипт
  `apps/api/src/db/backup.ts` (`pg_dump` → gzip-стрим → загрузка в S3
  `backups/grammashop-YYYY-MM-DD.sql.gz`, затем очистка по ретеншну).
  Логика отбора ключей на удаление вынесена в чистую функцию
  `backup-retention.ts` (последние 7 дампов безусловно + по одному
  самому свежему в каждой из следующих 4 отдельных ISO-недель) — покрыта
  vitest-тестами, не требующими БД/S3. Раннер — systemd timer
  (`docker/systemd/grammashop-backup.{service,timer}`, 03:00 ежедневно),
  вызывает `docker compose exec api node apps/api/dist/db/backup.js`, тот
  же паттерн, что и у `verify:s3`. Установка systemd-юнитов на VM —
  разовая ручная операция (не часть CI-деплоя, см. запись ниже).
  Раннтайм-образ `apps/api` получил `postgresql16-client` (Alpine-пакет,
  версия синхронизирована с `postgres:16-alpine`) — иначе в контейнере
  нет `pg_dump`/`psql`.
- [x] ~~Weekly restore-check: job разворачивает последний дамп в чистую БД
  и проверяет успешность; результат виден (лог/уведомление), а не
  молчит~~ — сделано 17.07.2026: `apps/api/src/db/restore-check.ts` —
  скачивает последний дамп из S3, разворачивает в отдельную БД
  `grammashop_restore_check` на том же Postgres (пересоздаётся с нуля
  перед каждым прогоном, боевую `grammashop` не трогает), проверяет, что
  в `public`-схеме появились таблицы. Провал — исключение в Sentry (не
  тихий лог) и ненулевой exit code, что видно в `journalctl` у systemd
  timer'а (`docker/systemd/grammashop-restore-check.{service,timer}`,
  воскресенье 04:00, через час после ночного бэкапа). Проверено на бою:
  оба таймера включены (`systemctl enable --now`), оба сервиса
  прогнаны вручную через `systemctl start` — `backup.service` выгрузил
  `backups/grammashop-2026-07-17.sql.gz` в S3, `restore-check.service`
  тут же успешно развернул именно этот дамп и подтвердил `restore-check:
  ok` в журнале.
  **Веха 7 дорожной карты закрыта целиком** — см. запись ниже.

Параллельно со спринтом (не код): онбординг мерчанта ЮKassa на ИП — см.
пункт в «Очереди».

### Спринт 9: первый видимый прототип

Цель: витрина seed-продавца открывается в ТМА (и в браузере в
dev-режиме) и показывает его товары — рабочая база, на которой дальше
идёт вся отладка.

Скоуп сознательно read-only со стороны покупателя, товары приходят из
seed-скрипта, не из UI. Изначальный план спринта включал и
продавцовскую админку (CRUD карточек) — при утверждении 18.07.2026
вырезана в отдельный спринт (кандидат в Спринт 10, см. «Очередь»): это
самостоятельный блок размером с авторизацию, витрине всё равно, откуда
в БД товары, а спринт из 10 задач противоречит правилу «один присест».
Также в следующих спринтах, не здесь: корзина/чекаут; регистрация с
оплатой подписки (блокируется онбордингом мерчанта ЮKassa — он идёт
параллельно, см. «Очередь»); фото товаров (пайплайн изображений —
отдельная неспроектированная задача очереди); платформенная админка.

Задачи в порядке зависимости:

- [x] ~~Платформенный Telegram-бот: создать через BotFather, включить
  Mini App (direct link на прод-домен), `BOT_TOKEN` в `.env` локально и
  на VM — без него не проверить подпись `initData`~~ — сделано
  18.07.2026: бот `@grammashopbot`, direct-link Mini App
  `t.me/grammashopbot/shop` (поддерживает `startapp` — заход на витрину
  по `seller_id`), Web App URL `https://grammashop.online`, Same-Origin
  Restriction оставлен включённым (прод-приложение того же origin).
  `TELEGRAM_BOT_TOKEN` вписан в `.env` локально и на VM (по одному
  заполненному значению с каждой стороны — проверено).
- [x] ~~Доменная схема в коде: таблицы Drizzle по
  [STACK.md#доменная-схема-v1](STACK.md#доменная-схема-v1), миграция
  взамен `health_check`, Zod-схемы и enum'ы статусов/тарифов в
  `packages/shared`~~ — сделано 18.07.2026: 8 таблиц + 6 enum'ов
  (`apps/api/src/db/schema.ts`), значения статусов/тарифов — единый
  источник в `packages/shared/src/domain/enums.ts` (Zod-схемы + массивы
  для `pgEnum`). Миграция-baseline пересобрана с нуля
  (`drizzle/0000_domain_schema_v1.sql`, старая заглушечная 0000 удалена —
  реальных данных нигде не было), в неё добавлен `DROP TABLE IF EXISTS
  health_check` для чистки уже развёрнутого прода (на свежих БД no-op).
  `health.service` переведён с вставки в `health_check` на пробу
  `select 1`. Проверено: `docker compose down -v` → миграция создаёт 8
  таблиц/6 enum'ов с FK-каскадами (`order_items.order_id` CASCADE,
  `variant_id` SET NULL), `pnpm -r typecheck` и `pnpm -r test` зелёные.
- [x] ~~Seed-скрипт продавца-разработчика: продавец + активная подписка
  Тарифа 1 + несколько товаров с вариантами (цена / старая цена /
  остаток) — временный мост, пока нет регистрации с оплатой (пункт 3
  продуктовой карты) и продавцовской админки (кандидат в Спринт 10)~~ —
  сделано 18.07.2026: `apps/api/src/db/seed.ts` (`pnpm --filter
  @grammashop/api db:seed`), параметризован по env
  (`DEV_SELLER_TELEGRAM_ID` числом у @userinfobot — с заглушкой
  `999000001`, если не задан; `DEV_SELLER_TELEGRAM_USERNAME`).
  Идемпотентен (перезапуск сносит прежнего dev-продавца по telegram_id и
  создаёт заново, чужих не трогает), guard против NODE_ENV=production.
  Каталог покрывает все состояния витрины: 3 товара / 6 вариантов —
  скидка (old_price), «нет в наличии» (stock 0), учёт остатка выключен
  (null), товар с единственным вариантом. Проверено на локальной БД.
- [x] ~~Авторизация, бэк (TDD): проверка HMAC `initData`, выдача JWT,
  резолвинг роли по Telegram ID (env-список админов);
  `@fastify/rate-limit` на auth сразу (пункт из ревью в «Очереди»)~~ —
  сделано 18.07.2026, дизайн зафиксирован в
  [STACK.md#авторизация](STACK.md#авторизация). Код: верификатор подписи
  `auth/init-data.ts` (timing-safe, окно 24ч), `services/auth.service.ts`
  (резолв `sellerId`+`isAdmin`, blocked не получает продавца),
  `routes/auth.route.ts` (`POST /auth` → JWT на 1ч через `@fastify/jwt`,
  rate-limit 20/мин), контракт в `packages/shared` (`authRequest/
  ResponseSchema`). TDD: 5 юнит-тестов верификатора + 8 интеграционных
  роута (покупатель/продавец/blocked/админ/подделка/протухший/400/429) —
  RED до кода, GREEN после; всего в api 18 зелёных. Env:
  `JWT_SECRET`+`ADMIN_TELEGRAM_IDS` заведены локально и на VM,
  проброшены в контейнер `api` через `docker-compose.yml` (вместе с
  `TELEGRAM_BOT_TOKEN`), фолбэки для CI — в `test/setup.ts`.
- [x] ~~Dev-режим авторизации: mock `initData` для браузера вне Telegram —
  иначе прототип отлаживается только внутри Telegram-клиента~~ — сделано
  18.07.2026, дизайн в [STACK.md#авторизация](STACK.md#авторизация)
  («Dev-режим»). Флаг `AUTH_DEV_MODE=true` (только вне production): `/auth`
  принимает mock-`initData` без валидной HMAC-подписи, доверяя `user.id`
  (токен бота вне Telegram недоступен — подписать нечем). Байпас изолирован
  в `auth/dev-mode.ts` (единственное место в бэке без проверки подписи), два
  предохранителя от прода: флаг мёртв при `NODE_ENV=production` + fail-fast
  в `buildApp()`. TDD: 11 юнит-тестов `dev-mode.test.ts` (флаг/предохранитель/
  разбор) + 3 интеграционных в `auth.route.test.ts` (dev-приём, резолв
  продавца, падение старта в проде), плюс `parseTelegramUser` вынесен из
  `verifyInitData` для переиспользования без дублирования. Проверено живьём
  curl'ом: mock без токена бота → JWT, мусор → 401, prod+флаг → старт падает.
  Мок `window.Telegram.WebApp` на фронте — вместе с чтением `initData` в
  задаче «Авторизация, фронт» ниже (там появится его потребитель).
- [x] ~~Dev-бот + туннель для дизайн-цикла: отдельный бот через BotFather
  (не платформенный), `cloudflared tunnel` с локального dev-server,
  HTTPS-урл туннеля в Mini App dev-бота — проверка Telegram-специфики
  (safe areas, `viewport`, клавиатура, `themeParams`) с телефона без
  деплоя на прод~~ — сделано, см.
  [STACK.md#локальный-дизайн-цикл-и-dev-бот](STACK.md#локальный-дизайн-цикл-и-dev-бот).
  Тулинг (18.07.2026): `cloudflared` установлен (`~/.local/bin`), Vite
  dev-server готов к туннелю — `server.host`, `strictPort` на 5173,
  `allowedHosts` для `*.trycloudflare.com`/`*.ngrok*`, прокси `/api` →
  `api` same-origin как прод-Caddy (`vite.config.ts`); api-client в dev по
  умолчанию бьёт same-origin `/api` (`lib/api-client.ts`); `AUTH_DEV_MODE`+
  `NODE_ENV=development` прокинуты в контейнер `api` через
  `docker-compose.override.yml` (прод-профилю недоступны); весь цикл — одним
  скриптом `scripts/dev-tunnel.sh`. Dev-бот `@grammashopdevbot` заведён.
  Проверено с телефона 19.07.2026: Mini App открылся внутри Telegram,
  витрина-скелет отрисовалась, `/health` подтянулся сквозь туннель
  (`ok`) — цепочка «локалка → cloudflared → host-Vite → приложение +
  прокси `/api` → docker-api» рабочая.
- [x] ~~Авторизация, фронт: чтение `initData` из Telegram SDK,
  axios-интерцептор JWT, гварды трёх групп маршрутов по роли,
  подключение TanStack Query~~ — сделано 19.07.2026 по TDD (5 шагов,
  21 тест), реализация в [STACK.md#авторизация](STACK.md#авторизация),
  «Фронт». Тонкая обёртка над `window.Telegram.WebApp` (`lib/telegram.ts`)
  вместо тяжёлого SDK; `resolveInitData()` (`auth/init-data.ts`) в браузере
  вне Telegram отдаёт mock — потребитель `AUTH_DEV_MODE`, ради которого тот
  делался. Токен — в памяти (`auth/token-store.ts`), не localStorage; axios
  на 401 разово молча пере-авторизуется и повторяет запрос (`lib/api-client.ts`).
  `AuthProvider` бутстрапит сессию до рендера маршрутов (loading/error),
  `useSession`+`SessionContext` (`auth/session-context.ts`); гварды
  `RequireSeller`/`RequireAdmin` и лендинг по `start_param`/роли
  (`auth/guards.tsx`, `auth/Landing.tsx`). `QueryClient` подключён
  (`lib/query-client.ts`, `main.tsx`). `telegram-web-app.js` в index.html.
  Попутно: RTL не делал авто-cleanup (в vitest нет `globals`) — DOM тёк
  между тестами, добавлен явный `cleanup` в `test/setup.ts`. Проверено с
  телефона: дев-бот лендит seed-продавца в «Админку продавца» (реальный
  `initData` → сессия → роль → лендинг).
- [x] ~~Витрина read-only: каталог по `seller_id` из `start_param` +
  карточка товара — первая вёрстка по
  [STACK.md#дизайн-направление](STACK.md#дизайн-направление)~~ — сделано
  19.07.2026. Бэк (TDD, 6 тестов): `GET /shop/:sellerId` — декоратор
  `fastify.authenticate` (JWT-гейт для доменных роутов, `app.ts`),
  контракт `packages/shared/src/schemas/shop.ts`, сервис
  `services/shop.service.ts` (active-продавец + active-товары с вариантами,
  **без ПДн продавца** — только shopName/описание/username, деньги в
  копейках). Фронт (скилл `frontend-design`): тема из Telegram `themeParams`
  через CSS-переменные `--tg-*` (`index.css`), стекло только на шапке
  (`.tg-glass`); `useShopCatalog` (TanStack Query), `shop/seller-id.ts`
  (start_param + dev-фолбэк `VITE_DEV_SELLER_ID`), витрина
  (`routes/storefront/StorefrontHome.tsx`) и карточка товара
  (`ProductDetail.tsx`) с состояниями (скидка зачёркнутой ценой, «нет в
  наличии», «от N ₽» при разбросе цен); утиль `lib/money.ts` и
  `shop/pricing.ts` — по TDD. Проверено headless-браузером (Playwright из
  кеша): витрина и карточка рендерятся сквозь весь стек (auth → каталог),
  скидка/распродано отрабатывают, тема применяется и в light, и в dark,
  горизонтального переполнения нет. Всего 70 тестов (api 39 + web 31).
- [x] ~~Сквозная проверка на бою: `t.me/<бот>/app?startapp=<seller_id>` с
  телефона открывает витрину с seed-товарами~~ — сделано 19.07.2026.
  Задеплоено в прод пушем `main` (коммит `c1942db`, CI test→build→deploy
  зелёный) — доменная миграция `0000_domain_schema_v1` впервые прогнана на
  проде (8 таблиц, `health_check` удалена), риск того, что drizzle сочтёт
  старую заглушечную 0000 уже применённой и пропустит новую схему, не
  реализовался, проверено по SSH (`\dt`, лог `migrations applied`).
  Демо-продавец засеян на проде вручную (`SEED_ALLOW_PROD=1`,
  `DEV_SELLER_TELEGRAM_ID=278003862` — владелец платформы, см.
  [project_owner_telegram_id]): продавец #1 (@syzrp), 3 товара/6 вариантов.
  Проверено с телефона через **платформенного** бота `@grammashopbot`
  (не dev-бот) — `t.me/grammashopbot/shop?startapp=1`: витрина «Демо-магазин»
  открылась, тёмная тема клиента подхватилась, стеклянная шапка на месте.
  Боевой путь целиком: реальный `initData` → прод `/auth` (проверка HMAC
  настоящим токеном бота, без `AUTH_DEV_MODE`) → `GET /api/shop/1` → каталог.
  `/api/health` и auth-гейт (`/api/shop/1` без токена → 401) отдельно
  подтверждены curl'ом с локалки. **Демо-продавец #1 на проде — временные
  данные**, снести перед реальным первым продавцом (см. запись в «Очереди»).

**Спринт 9 закрыт** — все задачи выполнены, вехи 1-7 инженерного трека и
первые 5 задач продуктовой карты (доменная схема → авторизация →
read-only витрина) сквозно проверены на бою.

### Спринт 10: дизайн-система платформы

Цель: платформенная визуальная идентичность (палитра, типографика,
символ/логотип) зафиксирована как чистовик — не отчёт о реализации в
коде, а спецификация, от которой дальше отталкивается и вёрстка
интерфейса, и кастомизация продавцов на Тарифах 2-3 (см.
[CONCEPT.md#кастомизация-витрины](CONCEPT.md#кастомизация-витрины)).

- [x] ~~Референсы: конкуренты + азиатская fashion-индустрия~~ — сделано
  19.07.2026. Снято headless-браузером вживую (не по памяти): Markaware
  (JP), 29CM, W Concept, Ader Error (KR) — мобильный вьюпорт, как у
  реального продукта.
- [x] ~~Черновик 1: тихий минимализм (Ink/Paper/Brass, Fraunces)~~ —
  сделано и **отклонён целиком** по решению 19.07.2026 (жирные прямые
  шрифты вместо тонкой антиквы, другая палитра). История и отклонённые
  гипотезы символа (весы, монета с самолётиком, змея с пачкой денег,
  гиря хендкодом) — в
  [docs/design/DESIGN_SYSTEM.md#история](design/DESIGN_SYSTEM.md#история).
- [x] ~~Поворот на Y2K: палитра, типографика, приёмы~~ — сделано
  19.07.2026, заземлено на исследовании тренда (не по памяти). Void/
  Chrome/Magenta/Ice, шрифт Unbounded (вариативный, с кириллицей —
  в отличие от Fraunces), хром-градиент на вордмарке, скан-линии
  фоном. Ice получил активную функциональную роль (фокус, статус,
  навигация) отдельно от magenta (коммерция/цена) — не декоративное
  дублирование двух акцентов.
- [x] ~~Символ/логотип: сгенерирован, не хендкодом~~ — сделано
  19.07.2026. Форма (гиря/калибровочный груз) была верной ещё в ручных
  SVG-набросках, но нормальный хромированный рендер получился только
  через AI-генерацию (Recraft/аналог) — не через ручной подбор
  bezier-координат. Проверено: фон удалён (самостоятельный ассет),
  держится на фонах Void/Chrome/белом, держится вплоть до 16px
  (favicon-размер) — проверено рендером, не на глаз.
- [x] ~~Зафиксировать в документации~~ — сделано 19.07.2026:
  [docs/design/DESIGN_SYSTEM.md](design/DESIGN_SYSTEM.md) (токены,
  типографика, приёмы, правила использования символа, готовый CSS),
  [docs/design/logo.svg](design/logo.svg) (ассет),
  [STACK.md#дизайн-направление](STACK.md#дизайн-направление) (короткая
  сводка + указатель), правка в
  [CONCEPT.md#кастомизация-витрины](CONCEPT.md#кастомизация-витрины).

**Спринт 10 закрыт** — дизайн-система зафиксирована как чистовик.
**Важно: в коде `apps/web` не применена** — интерфейс всё ещё на
системе Спринта 9 (brass/Fraunces). Перевод на новую систему — задача
в «Очереди» ниже, отдельный будущий спринт, не часть Спринта 10.

### Спринт 11: продавцовская админка (товары)

Цель: продавец может завести/отредактировать/удалить карточку товара с
вариантами через собственную админку — товар тут же виден на витрине.
Приоритет и состав — по ревью [2026-07-19](reviews/2026-07-19.md)
(рекомендация 4, код пп. 1-2): админка на критическом пути v1 (пункт 4
продуктовой карты), а не перевод дизайна (тот без того завязан на ещё
не спроектированный пайплайн изображений — см. «Очередь»). Две
однострочные правки из того же ревью встроены в начало, а не вынесены
отдельным спринтом — обе логически предшествуют CRUD. Задачи идут в
порядке зависимости.

- [x] ~~`trustProxy: true` в `app.ts` (или CIDR docker-сети) — Fastify за
  Caddy без него видит IP прокси-контейнера, rate limit на `/auth`
  делит один счётчик 20/мин на всех пользователей~~ — сделано
  19.07.2026: `Fastify({ trustProxy: true })` в `apps/api/src/app.ts`,
  безопасно — Caddy единственная точка входа снаружи (порты
  api/postgres/web не публикуются в `docker-compose.prod.yml`).
  `AUTH_RATE_LIMIT_MAX` в дев-цикле не тронут — не завязан на IP.
  39 тестов api зелёные.
- [x] ~~`.$onUpdate(() => new Date())` на `updated_at` во всех таблицах
  схемы~~ — сделано 19.07.2026, 5 полей (`sellers`, `subscriptions`,
  `products`, `productVariants`, `orders`) в `apps/api/src/db/schema.ts`.
  Чисто JS-хук Drizzle, DDL не меняется — подтверждено `drizzle-kit
  generate` («No schema changes»), миграция не нужна.
- [x] ~~Бэк, TDD: `routes → services → db` для CRUD товаров и вариантов
  (цена / старая цена / остаток), лимиты 30 карточек и 10 вариантов на
  карточку — в сервис-слое, не в БД-констрейнтах~~ — сделано
  19.07.2026. Контракт `packages/shared/src/schemas/products.ts`
  (create/update запросы, вариант input/update, ответы). RED-тесты
  первыми (`apps/api/src/routes/products.route.test.ts`, 17 кейсов) —
  все 404 до кода, GREEN после: `services/products.service.ts`
  (`listSellerProducts`/`createProduct`/`updateProduct`/`deleteProduct`/
  `addVariant`/`updateVariant`/`deleteVariant`, лимиты и ownership по
  `sellerId` — чужая карточка отдаёт 404, не 403, не палит факт
  существования) + `routes/products.route.ts`
  (`/seller/products[/:id[/variants/:variantId]]`, `preHandler:
  fastify.authenticate` + 403 без `sellerId` в JWT). Правило «у карточки
  минимум 1 вариант» (см. CONCEPT.md#каталог-и-заказы) — `deleteVariant`
  отказывает 400 на последнем варианте. Всего в api 56 тестов (было 39).
- [x] ~~Фронт: экраны продавцовской админки — список товаров, форма
  создания/редактирования, удаление~~ — сделано 19.07.2026. Список
  (`routes/seller/SellerHome.tsx`) — счётчик N/30, кнопки
  «Изменить»/«Удалить» (с `confirm`). Одна форма на создание и
  редактирование (`routes/seller/ProductForm.tsx`, роуты
  `/seller/products/new` и `/seller/products/:productId/edit`) — рубли в
  UI, копейки в контракте (`lib/money.ts`,
  `rublesToKopecks`/`kopecksToRubles`). Реконсиляция вариантов при
  редактировании — чистая функция `seller/variant-diff.ts` (по TDD, 5
  тестов): сравнивает исходные варианты с формой и решает, что уйдёт как
  `addVariant`/`updateVariant`/`deleteVariant` (эндпоинты принимают по
  одному варианту, не список целиком). Без фото — пайплайн изображений
  спроектирован отдельно (см. «Очередь»). Проверено вживую headless-браузером
  (Playwright, dev-сервер + локальный Postgres, `AUTH_DEV_MODE`): список
  seed-товаров, создание нового с одним вариантом, редактирование (правка
  цены существующего варианта + добавление второго — оба применились
  раздельными вызовами), удаление с подтверждением — весь цикл через
  реальный API, без моков. Всего в web 36 тестов (было 31).
- [x] ~~Сквозная проверка на бою: товар, заведённый через админку на
  проде, виден на витрине с телефона~~ — сделано 19.07.2026 (закрывает
  пункт 4 продуктовой карты). `git push origin main` → CI зелёный
  (test→build→deploy), с телефона заведена карточка «Заебатые штаны»
  (варианты S/M) через продавцовскую админку на проде — тут же видна на
  витрине (`t.me/grammashopbot/shop?startapp=1`). По пути найден и
  исправлен баг проверки вживую: вариант с одинаковой старой/новой ценой
  (продавец не убрал поле после ввода) зачёркивался как скидка —
  `shop/pricing.ts` (`hasDiscount` требует `oldPrice > price`, TDD, 12
  тестов), заодно добавлен бейдж процента скидки в `ProductDetail.tsx`.
  Задеплоено отдельным коммитом, тоже проверено с телефона на той же
  карточке — S без зачёркивания, M с «-40%».

**Спринт 11 закрыт** — продавец заводит/редактирует/удаляет карточки
товара с вариантами через собственную админку, изменения сразу видны на
витрине; пункт 4 продуктовой карты закрыт целиком.

### Спринт 12: корзина и чекаут

Цель: покупатель собирает корзину на витрине и оформляет заказ — ФИО,
телефон, адрес, согласие на обработку ПДн, для Тарифа 1 вместо оплаты
картой — экран с реквизитами/контактом продавца; продавец получает
уведомление о новом заказе через платформенного бота, остаток
списывается автоматически. Закрывает п.6 продуктовой карты. Заблокированные
внешне пункты карты (п.3 — ЮKassa-подписка продавца, ждёт модерацию
мерчант-аккаунта) сознательно не в этом спринте. Задачи идут в порядке
зависимости: сначала бэк (TDD), затем фронт, затем сквозная проверка.

- [x] ~~Бэк, TDD: `POST /shop/:sellerId/orders` — создание заказа
  (снапшот позиций и цен на момент заказа, см.
  [STACK.md#доменная-схема-v1](STACK.md#доменная-схема-v1)), проверка
  наличия остатка на каждом варианте корзины, авто-списание остатка
  внутри той же транзакции; `@fastify/rate-limit` на роут заказа сразу
  (пункт из очереди — «Rate limiting и защита API», не после первого
  абьюза)~~ — сделано 19.07.2026. Контракт
  `packages/shared/src/schemas/orders.ts` (`createOrderRequest/
  ResponseSchema`); ответ сразу несёт `seller.telegramUsername`/
  `paymentDetails` — данные для экрана оплаты Тарифа 1 (задача фронта
  чекаута ниже берёт их отсюда, не из публичного каталога витрины, где
  ПДн/реквизитов продавца нет намеренно). RED-тесты первыми
  (`apps/api/src/routes/orders.route.test.ts`, 8 кейсов) — все 404 до
  кода. GREEN: `services/orders.service.ts` (`createOrder`) — вся
  проверка/списание остатка и запись заказа+позиций в одной транзакции
  Drizzle с `for("update")` на строках вариантов (блокировка от гонки
  двух параллельных заказов на один вариант), ownership варианта к
  `sellerId` проверяется через join с `products` (чужой вариант → 404,
  не 403 — тот же принцип сокрытия, что и в продавцовской админке).
  `routes/orders.route.ts` — rate-limit 20/мин (env
  `ORDERS_RATE_LIMIT_MAX`, тот же паттерн, что у `/auth`). Всего в api 64
  теста (было 56).
- [x] ~~Бэк, TDD: уведомление продавцу о новом заказе через платформенного
  бота (grammY, см. [STACK.md#фоновые-задачи](STACK.md#фоновые-задачи)) —
  фоновой job, не блокирует ответ API покупателю~~ — сделано 19.07.2026.
  Первое использование pg-boss в проекте (зависимости `grammy`+`pg-boss`
  добавлены в `apps/api`). `queue/client.ts` — синглтон `PgBoss` на
  `DATABASE_URL` (та же Postgres, без отдельного брокера, как и решено в
  STACK.md); `bot/client.ts` — ленивый `getBot()` (конструктор grammY не
  делает сетевых вызовов, падать на импорте модуля незачем).
  `notifications/order-notification.ts`: `formatOrderNotificationText`
  (чистая функция, TDD без БД) → `sendOrderNotification` (грузит
  заказ+продавца+позиции, шлёт сообщение) → `enqueueOrderNotification`
  (кладёт `{orderId}` в очередь; ошибка отправки в очередь **не
  пробрасывается** — покупатель не должен получить 500 из-за insert в
  чужую от заказа схему pg-boss, провал уходит в Sentry, тот же принцип,
  что у weekly restore-check из Спринта 8) → `registerOrderNotificationWorker`
  (`boss.work`, запускается один раз в `index.ts` после `boss.start()`,
  тот же процесс, что и Fastify-сервер). `services/orders.service.ts`
  зовёт `enqueueOrderNotification` вне транзакции заказа и только при
  `ok: true` — энкью после коммита, не до. TDD:
  `notifications/order-notification.test.ts` (8 кейсов: форматирование,
  отправка через мок `getBot`, энкью через шпион на `boss.send`,
  устойчивость к падению очереди) — RED (module not found) → GREEN.
  Всего в api 70 тестов (было 64).
- [x] ~~Фронт: корзина на клиенте — context + reducer (см.
  [STACK.md#роутинг](STACK.md#роутинг)), добавление/удаление позиций с
  витрины и карточки товара, счётчик и мини-корзина в UI~~ — сделано
  19.07.2026. Чистый reducer TDD-первым (`cart/cart-reducer.ts`, 11
  тестов): позиция ключуется по `variantId`, количество зажато остатком
  варианта (`stock: null` — без верхней границы), добавление товара
  другого продавца сбрасывает корзину — заказ оформляется одному
  продавцу (см. CONCEPT.md#каталог-и-заказы). `cart/cart-context.tsx` —
  обёртка в контекст тем же приёмом, что и `auth/session-context.ts`,
  подключена в `main.tsx` между `AuthProvider` и роутером. UI: кнопка «В
  корзину» на карточке витрины — только для товара с единственным
  вариантом (несколько вариантов — через переход в карточку, там свой
  выбор), степпер количества на каждом варианте в `ProductDetail`,
  плавающая `MiniCartBar` (счётчик + сумма, ведёт на новый экран
  `/cart` — `CartPage.tsx`, список позиций с правкой количества/удалением
  и итогом). Кнопка «Оформить заказ» на `/cart` ведёт на `/checkout` —
  реализация того экрана: следующая задача этого спринта.
- [x] ~~Фронт: форма чекаута — ФИО, телефон, адрес, комментарий, чекбокс
  согласия на обработку ПДн (без чекбокса кнопка оформления неактивна);
  для Тарифа 1 — экран с реквизитами/контактом продавца вместо формы
  оплаты (см.
  [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы)). Текст
  согласия — рабочий черновик, финальная формулировка ждёт юрпакет
  152-ФЗ (см. «Очередь») — не блокер для кода~~ — сделано 19.07.2026.
  Сборка тела запроса — чистая функция `checkout/build-order-request.ts`
  (TDD, 5 тестов): позиции идут из корзины (`variantId`+`quantity`), поля
  покупателя подрезаются, пустой комментарий → `null`, отправка без
  `consent: true` бросает (не просто UX-подсказка — согласие обязательно
  по 152-ФЗ, кнопка отправки задизейблена без чекбокса тем же условием).
  `checkout/useCreateOrder.ts` — мутация TanStack Query, тот же приём,
  что у продавцовской админки. `routes/storefront/CheckoutPage.tsx`
  (роут `/checkout`) — форма → успех: экран с суммой заказа, реквизитами
  продавца (`seller.paymentDetails`) и кнопкой в его личку
  (`t.me/username`) — данные из ответа `/orders`, не из публичного
  каталога (там ПДн/реквизитов продавца нет намеренно, см.
  `shop.ts`). Корзина очищается после успешного оформления.
  Донастроено 20.07.2026: полевая валидация формы вместо единственного
  общего сообщения об ошибке — `checkout/validate-checkout-form.ts` (TDD,
  7 тестов) валидирует той же Zod-схемой, что и бэк
  (`checkoutFormSchema` — часть `createOrderRequestSchema`, вынесена в
  `packages/shared/src/schemas/orders.ts` специально ради переиспользования
  на фронте, см. STACK.md#валидация: «валидация не дублируется»).
  Обсуждались Yup+Formik — отклонены: Yup дублировал бы уже принятый Zod,
  Formik не соответствует стилю остальных форм проекта (обычный
  `useState`, как в `ProductForm.tsx`) и не оправдан размером формы (4
  поля + чекбоксом). Дальше в тот же день 20.07.2026, по фидбэку с
  проверки на бою: (1) телефон — формат только РФ (`+7XXXXXXXXXX`,
  `RU_PHONE_REGEX` в `checkoutFormSchema` — международный не нужен
  текущей аудитории v1); (2) валидация в реальном времени —
  `CheckoutPage.tsx` держит `touched`-состояние на каждое поле, обводка
  зеленеет сразу по мере ввода валидного значения (не дожидаясь потери
  фокуса/отправки), попытка отправки с ошибками помечает touched сразу
  по всем полям. Проверено headless-браузером (Playwright, dev-сервер +
  локальный Postgres, `AUTH_DEV_MODE`) — сквозь весь стек, не моками: по
  пути найден и исправлен баг — ошибка на чекбоксе согласия показывала
  сырое сообщение Zod («Invalid literal value, expected true») вместо
  дружелюбного текста, `validate-checkout-form.ts` для `consent`
  трактовал «пусто» только по строковому значению, булев `false` в эту
  ветку не попадал.
- [x] ~~Фронт: отображение ограничения ответственности платформы (оплата/
  доставка на Тарифе 1 — вне ТМА, по прямой договорённости сторон) в
  интерфейсе чекаута — переносится из «Очереди» сюда, логически
  относится к этому же экрану, а не отдельная задача~~ — сделано
  19.07.2026 вместе с формой выше. Текст-плашка (рабочий черновик, см.
  CONCEPT.md#коммуникация-продавца-и-покупателя — финальная формулировка
  ждёт юриста) показан дважды: в форме чекаута до оформления заказа (не
  только постфактум — ожидание выставлено заранее) и повторно на экране
  успеха рядом с реквизитами продавца.
- [x] ~~Сквозная проверка на бою: оформление заказа с телефона через
  `@grammashopbot`, продавец получает уведомление в Telegram, остаток
  на витрине уменьшается~~ — проверено 20.07.2026 с телефона: заказ
  оформлен через `@grammashopbot`, экран успеха с реквизитами продавца
  и переходом в его личку отработал, остаток на витрине списался
  корректно (проверено и в БД: варианты заказа ушли в 0). Уведомление
  продавцу **не дошло** — не баг кода (задача исправно встаёт в очередь
  pg-boss и обрабатывается воркером), а сетевая проблема самой прод-VM
  (не может достучаться до `api.telegram.org`) — диагностировано и
  зафиксировано в «Очереди» вместе со смежным открытым вопросом про
  саму механику доставки уведомлений произвольному продавцу (не только
  сетевая часть).

**Спринт 12 закрыт** — покупатель собирает корзину и оформляет заказ на
Тарифе 1 целиком (каталог → корзина → чекаут → экран успеха с
реквизитами продавца), остаток списывается автоматически; п.6
продуктовой карты закрыт. Уведомление продавцу о заказе реализовано и
корректно по коду, но не подтверждено на бою из-за внешней сетевой
проблемы — это открытый пункт в «Очереди», не блокер закрытия спринта
(сам чекаут-функционал от него не зависит).

### Спринт 13: заказы в продавцовской админке

Цель: продавец видит оформленные заказы и меняет их статус
(оплачен/выполнен/отменён) через свою админку — без этого вся механика
Тарифа 1 (продавец сам подтверждает оплату и выполнение, платформа не
проверяет факт оплаты, см.
[CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы)) не работает
на практике. Перенесено из «Очереди» (там же — контекст, почему это
приоритет, а не просто недостающая фича), закрывает п.7 продуктовой
карты. Задачи идут в порядке зависимости: бэк (TDD), затем фронт, затем
сквозная проверка.

- [x] ~~Бэк, TDD: `GET /seller/orders` — список заказов продавца (позиции
  со снапшотами, ФИО/телефон/адрес/комментарий покупателя, сумма,
  статус, дата), сортировка новые сверху; ownership строго по
  `sellerId` из JWT, тот же паттерн, что у `/seller/products` (чужие
  заказы просто не попадают в выборку, не 403).~~ — сделано 19.07.2026.
  Контракт `packages/shared/src/schemas/orders.ts`
  (`sellerOrderSchema`/`sellerOrderListResponseSchema`). RED-тесты первыми
  (`apps/api/src/routes/orders.route.test.ts`, новый блок `GET
  /seller/orders`) — 404 до кода. GREEN:
  `services/orders.service.ts` (`listSellerOrders`) + маршрут в
  `routes/orders.route.ts` (`requireSellerId`, тот же хелпер, что и в
  `products.route.ts`).
- [x] ~~Бэк, TDD: `PATCH /seller/orders/:id/status` — смена статуса.
  Разрешённые переходы (см. CONCEPT.md#каталог-и-заказы): из «новый» →
  «оплачен» или «отменён»; из «оплачен» → «выполнен» или «отменён»; из
  «выполнен» и «отменён» — никуда (терминальные статусы). Чужой/
  несуществующий заказ → 404; недопустимый переход → 400.
  `@fastify/rate-limit` не нужен — эндпоинт за JWT продавца, не публичный.~~
  — сделано 19.07.2026. Таблица переходов `ORDER_STATUS_TRANSITIONS`
  вынесена в `packages/shared/src/domain/enums.ts` — единый источник для
  бэка (валидация) и фронта (какие кнопки показывать), правило не
  дублируется (см. STACK.md#валидация). `services/orders.service.ts`
  (`updateOrderStatus`) — попутно реализован пункт из
  CONCEPT.md#каталог-и-заказы, не описанный явно в этой задаче: при
  переходе в «отменён» остаток варианта восстанавливается («возвращается
  при его отмене»), под той же блокировкой строк (`for("update")`), что и
  списание в `createOrder` — иначе параллельный заказ на тот же вариант
  мог бы разъехаться с отменой. Итого в api 80 тестов (было 70, +10 на
  этот блок).
- [x] ~~Фронт: экран списка заказов в продавцовской админке — карточка
  заказа (позиции, покупатель, сумма, статус), кнопки смены статуса
  (только допустимые переходы для текущего статуса, остальные скрыты,
  не задизейблены — по аналогии с недоступными переходами в CONCEPT).~~ —
  сделано 19.07.2026. `seller/useSellerOrders.ts` (TanStack Query, тот же
  приём, что и `useSellerProducts.ts`), `routes/seller/SellerOrders.tsx`
  (роут `/seller/orders`, ссылка туда добавлена в шапку `SellerHome.tsx`)
  — кнопки перехода строятся из общего `ORDER_STATUS_TRANSITIONS`, а не
  захардкожены повторно. Без отдельного компонент-теста — как и у
  `SellerHome`/`ProductForm` раньше, эти экраны проверяются вживую, не
  RTL-снапшотом.
- [x] ~~Сквозная проверка на бою: заказ, оформленный покупателем на
  прошлой сквозной проверке (Спринт 12), виден в списке продавца;
  смена статуса на «оплачен» → «выполнен» отражается сразу.~~ —
  подтверждено с телефона 19.07.2026 (после `git push origin main`,
  коммит `15df10e`, CI test→build→deploy зелёный): заказ виден в
  `/seller/orders`, смена статуса отрабатывает на бою.
- [x] ~~Откат статуса на шаг назад — «оплачен» → «новый» (отмена оплаты) и
  «выполнен» → «оплачен» (снять отметку о выполнении)~~ — добавлено
  19.07.2026 по фидбэку сразу после проверки на бою (продавцу нужно
  исправлять ошибочный клик, не только двигаться вперёд/отменять
  целиком). Решение зафиксировано в
  [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы): «отменён»
  остаётся терминальным без отката (в отличие от оплаты/выполнения — это
  собственные пометки продавца, ничего вовне не триггерящие). Код:
  расширена только таблица `ORDER_STATUS_TRANSITIONS`
  (`packages/shared/src/domain/enums.ts`) — ни сервис (`updateOrderStatus`),
  ни бэк-роут менять не пришлось, транзиты уже читались из общей таблицы.
  Фронт: `TRANSITION_LABELS` в `SellerOrders.tsx` переведён с
  `Record<OrderStatus, string>` на `Record<OrderStatus, Partial<Record<OrderStatus, string>>>`
  — метка кнопки теперь зависит от пары (откуда, куда), не только от
  цели («оплачен» как цель из «новый» — это отметка оплаты, а из
  «выполнен» — откат). RED→GREEN: 3 новых теста в
  `orders.route.test.ts` (оплачен→новый; выполнен→оплачен; отменён
  остаётся без отката) — итого в api 83 теста (было 80). Проверено
  живьём (dev-сервер, реальные запросы): «выполнен» → «снять отметку» →
  «Оплачен» → «отменить оплату» → «Новый», кнопки на каждом шаге
  правильные.

**Спринт 13 закрыт** — продавец видит заказы, меняет статус вперёд и
откатывает ошибочный клик назад, «отменён» — терминальный статус; п.7
продуктовой карты закрыт целиком.

Два визуальных бага найдены на самой боевой проверке с телефона, исправлены
тем же днём 19.07.2026:
- **Кнопки смены статуса вылезали за карточку** при 3 одновременных кнопках
  (`оплачен` даёт откат + вперёд + отмену) — ряд кнопок не переносился.
  Исправлено: сумма заказа и кнопки разнесены по отдельным строкам,
  `flex-wrap` на ряду кнопок (`SellerOrders.tsx`).
- **Текст `outline`-кнопок сливался с фоном в тёмной теме** — у варианта
  `outline` (`components/ui/button.tsx`) фон был явно статичным
  (`bg-background`, всегда светлый — см. STACK.md#дизайн-направление:
  админки специально плоские, без темы клиента), а цвет текста не был
  задан явно и наследовался от `body` (`--tg-text`), который в тёмной теме
  Telegram становится светлым — светлый текст на светлом фоне кнопки.
  Исправлено добавлением `text-foreground` к варианту `outline` — пара
  фон/текст стала самодостаточной, не зависящей от темы клиента. Заодно
  чинит ту же проблему у кнопки «Изменить» в `SellerHome.tsx` (тот же
  компонент). Проверено живьём в браузере с симуляцией тёмной темы
  Telegram (переопределены `--tg-theme-*` на `:root`) — обе кнопки-цели
  внутри границ карточки, контраст текста корректен.

### Спринт 14: платформенная админка

Цель: платформенный админ (владелец платформы) видит список продавцов со
статусом подписки и может вручную заблокировать/разблокировать магазин —
закрывает п.8 продуктовой карты, последний пункт дорожной карты v1, не
зависящий от внешних блокеров (ЮKassa-модерация/юрпакет — см. «Очередь»,
блокируют только п.3). Задачи идут в порядке зависимости: бэк (TDD),
затем фронт, затем сквозная проверка. Сформировано 20.07.2026.

- [x] ~~Бэк, TDD: `GET /platform/sellers` — список продавцов (магазин,
  Telegram-контакт, статус продавца, статус/тариф подписки, `paidUntil`)
  для платформенного админа. Доступ — `request.user.isAdmin`, не
  `sellerId`~~ — сделано 20.07.2026. Контракт
  `packages/shared/src/schemas/platform.ts` (`platformSellerSchema` —
  `subscription` nullable: продавец может существовать без строки в
  `subscriptions`, форма регистрации ещё не написана, п.3 продуктовой
  карты). `services/platform.service.ts` (`listSellers`) — `LEFT JOIN`
  `sellers`/`subscriptions`, не `INNER` (иначе продавец без подписки
  выпал бы из списка целиком). RED-тесты первыми
  (`apps/api/src/routes/platform.route.test.ts`) — все 404 до кода.
- [x] ~~Бэк, TDD: `PATCH /platform/sellers/:id/status` — ручная смена
  `active`↔`blocked`~~ — сделано 20.07.2026,
  `services/platform.service.ts` (`updateSellerStatus`), несуществующий
  продавец → 404. Всего в api 92 теста (было 84, +8 на платформенный
  роут). Заодно вынесен общий `requireAdmin`
  (`apps/api/src/auth/access.ts`) — не перепроверяет `isAdmin` по БД в
  отличие от `requireSellerId` ниже: список админов живёт в env
  (`ADMIN_TELEGRAM_IDS`), не в таблице, перепроверять нечего.
- [x] ~~Открытый вопрос — насколько строго отзывать сессию заблокированного
  продавца~~ — решено 20.07.2026 в пользу варианта (б) из развилки в
  «Очереди»: `requireSellerId` (раньше дублировался в
  `products.route.ts`/`orders.route.ts`, вынесен в общий
  `apps/api/src/auth/access.ts`) теперь перепроверяет статус продавца по
  БД на каждый запрос `/seller/*`, а не только наличие `sellerId` в JWT —
  заблокированный админом продавец теряет доступ немедленно, не дожидаясь
  истечения часового TTL токена. Лишний DB-хит на запрос — цена,
  приемлемая на масштабе v1. Зафиксировано в
  [STACK.md#авторизация](STACK.md#авторизация). RED-тест первым (новый
  кейс в `products.route.test.ts`: токен с `sellerId` выдан → продавца
  блокируют в БД напрямую → тот же токен → ожидается 403, не 200) — 84
  теста api до правки, 200 вместо ожидаемого 403, GREEN после.
- [x] ~~Фронт: экран списка продавцов в `/platform`~~ — сделано
  20.07.2026. `platform/usePlatformSellers.ts` (TanStack Query, тот же
  приём, что и `seller/useSellerOrders.ts`), `routes/platform/PlatformHome.tsx`
  — карточка продавца (магазин, `@username`, статус, тариф+статус
  подписки, дата `paidUntil`), кнопка «Заблокировать»/«Разблокировать» с
  `confirm` на блокировку (без confirm на разблокировку — низкий риск, не
  триггерит ничего необратимого, тот же принцип, что у отката статуса
  заказа в Спринте 13). Найдено и исправлено по пути: владелец платформы
  — одновременно продавец (`sellerId` + `isAdmin` в одной сессии, см.
  [project_owner_telegram_id]), а `Landing.tsx` при заходе без
  `start_param` проверяет `sellerId` раньше `isAdmin` и уводит его в
  `/seller` — без явной ссылки `/platform` был физически недостижим (в
  Mini App нет адресной строки для ручного перехода). Добавлена
  кросс-навигация: кнопка «Платформа» в шапке `SellerHome.tsx` (видна
  только при `session.isAdmin`) и «Мой магазин» в шапке `PlatformHome.tsx`
  (видна только при `session.sellerId != null`). Проверено headless-браузером
  (Playwright, dev-сервер + локальный Postgres, `AUTH_DEV_MODE`,
  `VITE_DEV_TELEGRAM_ID` владельца): список с seed-продавцом рендерится
  (магазин/контакт/тариф/дата), блокировка через UI отражается сразу
  (бейдж → «Заблокирован», кнопка → «Разблокировать»), `curl` к `/shop/4`
  тем же токеном подтвердил 200→404→200 на блокировку/разблокировку;
  переход `/seller` → «Платформа» → `/platform` → «Мой магазин» → `/seller`
  пройден вживую.
- [x] ~~Сквозная проверка на бою~~ — подтверждено 20.07.2026: `git push
  origin main` → CI (test→build→deploy) зелёный, коммит `bf8520d`. С
  телефона владелец (`syzrp`) открыл `@grammashopbot` без `startapp` →
  лендинг в `/seller` → кнопка «Платформа» → список продавцов
  (демо-магазин, Тариф 1, оплачена) → «Заблокировать» → подтверждение →
  бейдж «Заблокирован» → повторный заход на витрину демо-продавца
  (`?startapp=1`) отдал «магазин недоступен» → «Разблокировать» → витрина
  снова открылась.

**Спринт 14 закрыт** — п.8 продуктовой карты закрыт целиком, все 8
пунктов дорожной карты продукта v1 реализованы (п.3 — регистрация и
оплата подписки продавца — остаётся заблокирован внешне онбордингом
ЮKassa/юрпакетом, см. «Очередь», не кодом).

### Спринт 15: публичный лендинг

Цель: посетитель обычного браузера на `grammashop.online` видит
маркетинговый лендинг (не крашится и не видит ТМА), а вход из Telegram
(`t.me/grammashopbot`) продолжает открывать ТМА без изменений. Заведено
20.07.2026 по запросу пользователя — ценность выше косметики: прямой
инструмент для **внешней проверки v1**
([CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp), «первый внешний
продавец самостоятельно проходит путь без помощи разработчика»), без
канала, откуда посторонний продавец вообще узнаёт о платформе, этот шаг
неоткуда взять. Состав и оба архитектурных решения (root-урл остаётся за
ТМА, лендинг — статический файл за клиентским редиректом; CTA — диплинк
в бота, не форма лида) из того же обсуждения. Задачи идут в порядке
зависимости: контент → редирект → OG-теги → сквозная проверка.

- [x] ~~Статическая страница `apps/web/public/landing.html` (паттерн
  `offer.html`/`privacy.html` — простой HTML/CSS, без React-бандла):
  хиро с каруселью слов («Магазин для мерча / самозанятых / секонда и
  винтажа / хендмейда / курсов», вертикальный скролл CSS-анимацией),
  подзаголовок, крючок «без BotFather» сразу под хиро (см.
  [CONCEPT.md#конкуренты-и-дифференциация](CONCEPT.md#конкуренты-и-дифференциация)),
  «как это работает» (3 шага из
  [CONCEPT.md#как-это-работает](CONCEPT.md#как-это-работает)), тарифы
  (таблица из [CONCEPT.md#тарифы](CONCEPT.md#тарифы), Тарифы 2-3 с
  пометкой «рабочий ориентир», синхронно с `offer.html`), тезис
  «платформа не берёт долю с продаж», живой пример — ссылка на
  демо-магазин (`t.me/grammashopbot/shop?startapp=1`), FAQ, футер со
  ссылками на `offer.html`/`privacy.html` и `@grammashopbot`. CTA —
  `t.me/grammashopbot?text=...` (см. «Очередь», решение по CTA), не
  форма регистрации (её ещё нет, п.3 продуктовой карты заблокирован
  внешне)~~ — сделано 19.07.2026. Токены и типографика — по
  [docs/design/DESIGN_SYSTEM.md](design/DESIGN_SYSTEM.md) (Void/Chrome/
  Magenta/Ice, Unbounded): лендинг явно назван там айдентикой платформы
  наравне с админками, не витриной продавца — токены не подлежат
  кастомизации. Заодно закрыт побочный пробел из «Не реализовано» того
  же документа: `docs/design/logo.svg` скопирован в
  `apps/web/public/logo.svg` и подключён как favicon (был не подключён
  нигде в коде). Карусель слов — чистый CSS (`@keyframes`, без библиотек
  и без JS), первая попытка держала одну высоту строки на весь набор
  слов и обрубала более длинные фразы («секонда и винтажа», «курсов и
  цифровых товаров») переносом на вторую строку под `overflow: hidden`
  — исправлено измеренным (не угаданным) `font-size: clamp(...)`
  отдельно от заголовка: `Playwright`-скрипт мерил реальный
  `scrollWidth` самой длинной фразы против ширины контейнера на сетке
  вьюпортов 320-1280px, добивался нулевого переполнения на каждом, а не
  проверялся на глаз на одном размере экрана. Тем же способом найден и
  исправлен второй баг — на 320px шапка (`Тарифы`/`FAQ`) переносилась
  некрасиво посередине слова из-за нехватки места, пофикшено
  `flex-wrap` на `header.site`.
- [x] ~~Редирект вне Telegram: перехватить существующее исключение
  `resolveInitData()` («initData недоступен», см.
  `apps/web/src/auth/init-data.ts`) в проде вне Telegram — сейчас голый
  краш экрана — и сделать `window.location.replace("/landing.html")`.
  В dev эта ветка не должна срабатывать (там `resolveInitData()` отдаёт
  мок, а не бросает — проверить, что редирект действительно не мешает
  локальному дизайн-циклу без Telegram, не полагаться молча)~~ —
  сделано 19.07.2026 по TDD. `resolveInitData()` теперь бросает не
  голый `Error`, а именованный `InitDataUnavailableError`
  (`auth/init-data.ts`) — `AuthProvider` перехватывает именно его и
  делает `window.location.replace("/landing.html")`, любая другая
  ошибка (сеть, невалидный ответ) по-прежнему падает в общий экран «не
  удалось войти» — узкая проверка типа ошибки, не общий catch-всё.
  RED-тест первым (`AuthProvider.test.tsx`, `vi.spyOn` на
  `sessionModule.authenticate`) — редирект не вызывался, экран ошибки
  оставался. GREEN после правки. Dev-ветка не задета — новый тест в
  `init-data.test.ts` (`vi.stubEnv("DEV", false)`) подтверждает throw
  только вне dev, существующие 3 dev-теста (mock-initData) остались
  зелёными без изменений. Итого в web 68 тестов (было 64).
- [x] ~~OG-теги (заголовок/описание/картинка) — продублировать и в
  `index.html`, и в `landing.html`: превью-боты (Telegram, соцсети) не
  исполняют JS и видят только статический `<head>` того, что реально
  отдаётся на `/`~~ — сделано 19.07.2026, идентичный набор
  `og:title`/`og:description`/`og:image`/`twitter:card` в обоих файлах.
  Картинки для превью раньше не было вовсе (нет пайплайна
  изображений/растровых экспортов в проекте) — сгенерирована через
  headless-браузер: отдельная HTML-заготовка (Void-фон, хром-вордмарк,
  magenta-разделитель, ice-тэглайн — те же токены, что и у лендинга)
  отрендерена Playwright'ом в `apps/web/public/og-image.png` (1200×630,
  стандартный размер OG-превью). `pnpm exec vite build` проверен вручную
  — `landing.html`/`landing.css`/`logo.svg`/`og-image.png` копируются в
  `dist/` как есть (статика из `public/`), typecheck (`pnpm -r
  typecheck`) зелёный.
- [x] ~~Сквозная проверка на бою: `grammashop.online` в обычном браузере
  (не Telegram, с телефона или десктопа) редиректит на лендинг с рабочей
  каруселью и живой ссылкой на демо-магазин; `t.me/grammashopbot` из
  Telegram по-прежнему открывает ТМА без изменений (регрессия входа —
  главный риск задачи, проверить явно, не только новую страницу)~~ —
  сделано 19.07.2026. `git push origin main` (коммит `aa2dd05`) → CI
  (test→build→deploy) зелёный. Автоматически подтверждено
  headless-браузером против прода: заход на `https://grammashop.online/`
  вне Telegram уводит на `/landing.html` (реальный клиентский редирект,
  не мок), `landing.html`/`landing.css`/`logo.svg`/`og-image.png`
  отдаются 200, `og:*`-теги отданы на `/`. Вход через реальный Telegram
  подтверждён с телефона: `t.me/grammashopbot` по-прежнему открывает ТМА
  как обычно — регрессии нет (код успешного пути `resolveInitData()` не
  менялся, задет только новый catch-случай отсутствия initData).

**Спринт 15 закрыт** — публичный маркетинговый лендинг живёт на
`grammashop.online`, вход из Telegram не затронут. Второе условие
критерия готовности v1 ([CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp),
«первый внешний продавец самостоятельно проходит путь без помощи
разработчика») получило канал, откуда посторонний продавец вообще может
узнать о платформе — само прохождение пути по-прежнему блокировано
внешне (онбординг ЮKassa + юрпакет 152-ФЗ, см. «Очередь»), лендинг эту
блокировку не снимает, но убирает соседний недостающий кусок.

Донастроено 19.07.2026 по фидбэку сразу после закрытия спринта:
- Карусель слов расширена с 5 до 9 фраз — добавлены «художников»,
  «музыкантов», «татуировщиков», «артистов»; `@keyframes carousel`
  пересчитаны скриптом под новое число фраз (не руками — риск ошибиться
  в процентах слишком высок), длиннейшая фраза осталась той же
  («курсов и цифровых товаров»), но `font-size: clamp(...)` карусели
  всё равно перепроверен той же Playwright-методикой (сетка вьюпортов
  320-1920px, ноль переполнения), т.к. изменилась ширина контейнера
  (см. следующий пункт).
- Вёрстка переведена с одной узкой колонки (720px на всех размерах
  экрана) на настоящий mobile-first: `.page` (шапка/футер) тянется до
  1200px по мере роста вьюпорта, вложенный `.content` держит прозу и
  карточки в читаемых 820px — на широком мониторе страница выглядит
  спроектированной под экран, а не растянутым скриншотом телефона.
  `.steps` переведён на тот же `repeat(auto-fit, minmax(...))`, что уже
  был у `.pricing` (2-3 колонки на широких экранах). Побочный баг найден
  тем же Playwright-скриптом измерения (`li.scrollWidth`), не на глаз:
  `<code>` с URL-путём был flex-item-соседом текста (а не текстом,
  идущим следом), поэтому `display: block` на нём ничего не менял —
  переполнял узкую колонку при 2-3-колоночной сетке. Пофикшено обёрткой
  текста шага в `<span class="step-text">`, чтобы код действительно
  начинал новую строку внутри неё.
- `.hook` (текст под хиро) и акцентное слово в `.claim` переведены с
  `--ice` на `--text-dim`/`--magenta` соответственно — по фидбэку по
  балансу цвета после того, как ранее в этом же диалоге акцент был
  сделан на увеличение доли ice. Функциональные/навигационные
  употребления ice (ссылки, номера шагов, eyebrow-лейблы, ссылка на
  демо-магазин) не тронуты — только два больших нефункциональных блока
  текста.
- Статичный подзаголовок под хиро заменён на карусель из 21 слогана
  (тот же приём вертикального CSS-скролла, что и у карусели аудиторий
  в `h1`, но для целых фраз, не слов) — по фидбэку: первый черновик
  слоганов транслировал внутреннюю реализацию («без BotFather», «без
  токенов») вместо боли/желания продавца, отклонён целиком, 30 новых
  вариантов от боли клиента предложены и из них выбрано 21 — см.
  [CONCEPT.md#слоганы-лендинга](CONCEPT.md#слоганы-лендинга), там же
  зафиксировано решение и группировка по типу боли. Скиллы `copywriting`
  и `ogilvy-copywriting` (установлены ранее в этом же спринте) применены
  при подборе. Высота слайда карусели — с запасом на 3 строки
  (`--slide: 4.6rem`), не 2: Playwright-проверкой на сетке вьюпортов
  320-1920px обнаружено, что самый длинный слоган (56 символов) на 320px
  обрезался в 2-строчном слоте — та же методика измерения, что уже
  дважды спасала от переполнения в этом спринте, здесь поймала баг до
  деплоя, а не после. Побочная находка при вёрстке — `.hook` рядом всё
  ещё говорил «не нужно создавать своего бота... вставлять токены», та
  же внутренняя лексика, от которой только что отказались в слоганах —
  исправлено тем же днём: «Никакой технической возни — просто оформи
  подписку и получи готовую ссылку.» (принцип зафиксирован в
  [CONCEPT.md#принцип-формирования](CONCEPT.md#принцип-формирования)).
- Карусель слоганов перемещена из хиро в секцию «Тарифы» (между `h2` и
  сеткой тарифных карточек) — по фидбэку: две карусели подряд (аудитории
  в `h1`, слоганы под ним) выглядели как повтор одного приёма, а не два
  разных сообщения. Хиро остался с одной каруселью; `.slogan-carousel`
  не имеет жёсткой привязки к хиро — при переносе понадобилась только
  одна правка (добавить `text-align: center` явно, раньше центрирование
  наследовалось от `.hero`, в секции тарифов такого наследования нет).
  Высота слайда и переполнение перепроверены Playwright-скриптом на той
  же сетке вьюпортов — без изменений (ширина контейнера та же, `.content`
  ограничивает 560px в обоих местах).

## Дорожная карта проекта

Высший масштаб планирования: инженерный трек от пустого репозитория до
работающего деплоя на сервере. Это не про фичи продукта (подписки,
каталог, чекаут и т.д. — они появятся как задачи внутри веток «Бэкенд»/
«UX/UI» позже) и не про юридическо-организационные вещи (оферта,
юрлицо, пилотный запуск) — те заводятся отдельными задачами, когда до
них дойдёт очередь. Здесь — только базовые инструменты и порядок, в
котором они поднимаются, чтобы можно было работать на следующем уровне
абстракции. Веха 1 — фундамент; 2-4 — следующий слой, может идти
параллельно; 5-6 — финал. Редактируется по ходу работы.

- [x] **1. Инициализация проекта** — монорепо (pnpm workspaces), базовая
  структура `apps/web`/`apps/api`/`packages/shared`, `package.json` и
  TypeScript-конфиги в каждом пакете, `.env.example` и схема
  обязательных переменных окружения (БД, JWT-секрет, ключи S3 и т.д.),
  минимальный `.gitignore`.
- [x] **2. Докер и локальное окружение** — `docker-compose.yml`
  (postgres + тестовая БД/схема + api + web), multi-stage `Dockerfile`
  на каждый сервис, `docker compose up` поднимает всё локально и
  отдаёт "hello world" с api и web.
- [x] **3. База данных** — подключение Drizzle к Postgres, первая
  миграция, базовая схема (заглушка), проверка миграций в докере.
- [x] **4. Бэкенд** — скелет Fastify, пайплайн `routes → services → db`
  (см. `STACK.md#пайплайн-запроса`), Zod-схемы в `packages/shared`,
  первый health-check эндпоинт — по TDD (см. `STACK.md#тестирование`):
  сначала падающий тест через `fastify.inject()`, потом код.
- [x] **5. UX/UI** — скелет React + Vite, `shadcn/ui` подключён,
  роутинг трёх групп маршрутов (см. `STACK.md#роутинг`), axios
  настроен на обращение к `apps/api`, первый компонент — тоже по TDD
  (`@testing-library/react` + Vitest).
- [x] **6. CI/CD** — GitHub Actions: прогон тестов (Vitest, красные —
  пайплайн падает) → сборка Docker-образов → пуш в ghcr.io. SSH-деплой
  на VM отложен до вехи 7 (сервера ещё нет), см.
  `STACK.md#ci-cd`.
- [x] **7. Деплой на сервер** — VM в Yandex Cloud (старт — минимальная
  бюджетная конфигурация для отладки деплоя, апгрейд по факту нагрузки,
  см. [STACK.md#хостинг-и-деплой](STACK.md#хостинг-и-деплой)) ✅,
  `docker-compose` в проде с Caddy как reverse proxy (автоматический TLS
  через Let's Encrypt — обязателен, ТМА открывается только по HTTPS) ✅,
  S3-хранилище и Sentry подключены и проверены на бою ✅, nightly `pg_dump`
  в S3 настроен и еженедельная проверка восстановления работает (см.
  [STACK.md#бэкапы](STACK.md#бэкапы)) ✅.

## Дорожная карта продукта (v1)

Следующий уровень планирования после инженерного трека выше — фичи,
нужные для сквозного пути «продавец подписался → покупатель сделал
заказ» в объёме Тарифа 1 (см.
[CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp)). Пункты идут в
предполагаемом порядке зависимостей, порядок может уточняться по ходу —
это не скрам-доска на неделю вперёд, а следующий уровень дорожной карты
после инженерного трека (вехи 1-7 выше), который до этого момента нигде
не был явно разложен.

1. **Доменная схема** — таблицы Drizzle для продавцов, товаров с
   вариантами (карточка + варианты, цена на варианте, ≤10 вариантов на
   карточку — см.
   [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы)) и заказов
   со статусной моделью «новый → оплачен → выполнен / отменён» (взамен
   схемы-заглушки `health_check`, см. веху 3). Схема согласована
   18.07.2026 →
   [STACK.md#доменная-схема-v1](STACK.md#доменная-схема-v1) — осталось
   написать миграции.
2. **Авторизация** — проверка `initData` (HMAC), выдача JWT-сессии,
   резолвинг роли (покупатель/продавец/админ) по Telegram ID (см.
   [STACK.md#авторизация](STACK.md#авторизация)).
3. **Регистрация и подписка продавца** — форма регистрации, интеграция
   ЮKassa (автоплатежи), грейс-период 3 дня, скрытие витрины при
   неоплате (см.
   [CONCEPT.md#оплата-подписки-продавцом](CONCEPT.md#оплата-подписки-продавцом)).
   Сюда же встраивается идемпотентность вебхука ЮKassa (уже в очереди
   ниже) — не отдельный этап в конце. **Важное ограничение, вскрытое
   20.07.2026 при проверке уведомлений о заказах** (см. «Очередь»,
   «Уведомления продавцу через Telegram-бота» — сетевая проблема с
   прод-VM отдельно, это ограничение — про сам механизм, не про сеть):
   Telegram Bot API не даёт боту писать пользователю первым — только
   если пользователь сам хотя бы раз начал с ботом диалог/открыл его
   Mini App. Раз регистрация продавца в любом случае идёт через открытие
   `@grammashopbot`, это условие выполняется автоматически самим фактом
   регистрации — специально предусматривать ничего не нужно, но это
   именно предположение, которое стоит явно проверить при реализации
   формы регистрации (не полагаться молча), и учесть кейс «продавец
   заблокировал бота после регистрации» — `sendMessage` в этом случае
   вернёт 403, обрабатывается тем же путём, что и сетевые сбои (см.
   `notifications/order-notification.ts` — ошибка воркера не роняет
   покупательский флоу, видна как `failed` job в pg-boss).
4. **Продавцовская админка: товары** ✅ — CRUD карточек товара с лимитом
   30 шт. (Тариф 1, см. [CONCEPT.md#тарифы](CONCEPT.md#тарифы)) сделан
   Спринтом 11, проверено на бою с телефона. Загрузка изображений — вне
   этого пункта, в работе Спринтом 16.
5. **Витрина и каталог** ✅ — рендер витрины продавца по `seller_id` из
   `startapp`, список товаров, карточка товара — сделан Спринтом 9,
   проверено на бою с телефона.
6. **Корзина и чекаут** ✅ — сборка корзины, оформление заказа с полной
   формой (ФИО, телефон, адрес, комментарий) и согласием на обработку
   ПДн; на Тарифе 1 вместо формы оплаты картой — экран с
   реквизитами/контактом продавца (см.
   [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы)) — сделано
   Спринтом 12, проверено на бою с телефона. Уведомление продавцу о
   новом заказе через платформенного бота и авто-списание остатков —
   тоже реализованы; списание остатков подтверждено на бою, доставка
   уведомления — нет (внешняя сетевая проблема прод-VM, не код, см.
   «Очередь»). Rate limiting на создание заказов встроен сразу, не после
   первого абьюза.
7. **Продавцовская админка: заказы** ✅ — список заказов, смена статуса
   вперёд (оплачен/выполнен) и откат ошибочного клика назад — сделано
   Спринтом 13, проверено на бою с телефона.
8. **Платформенная админка** ✅ — список продавцов, статус подписки,
   ручная блокировка/разблокировка — сделано Спринтом 14, проверено на
   бою с телефона (блокировка демо-продавца сразу скрывает витрину,
   разблокировка возвращает).

Все 8 пунктов дорожной карты продукта v1 реализованы и проверены на бою.
Единственное открытое ограничение — п.3 (регистрация и оплата подписки
продавца) технически написан лишь частично (авторизация — да, форма
регистрации и интеграция ЮKassa — нет): блокер внешний, не инженерный
(модерация мерчант-аккаунта ЮKassa + юрпакет 152-ФЗ, см. «Очередь»), но
именно он не даёт объявить v1 полностью готовым к внешней проверке — см.
критерий готовности ниже.

Критерий готовности v1 (внутренняя + внешняя проверка) — см.
[CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp).

## Выполнено

- Тарифная сетка подписки → [CONCEPT.md#тарифы](CONCEPT.md#тарифы)
- Оплата подписки продавцом → [CONCEPT.md#оплата-подписки-продавцом](CONCEPT.md#оплата-подписки-продавцом)
- Целевой рынок и валюта → [CONCEPT.md#рынок](CONCEPT.md#рынок)
- Юридическая форма отношений платформа-продавец → [CONCEPT.md#юридическая-форма-отношений-с-продавцом](CONCEPT.md#юридическая-форма-отношений-с-продавцом)
- Лимиты на товары и анти-abuse меры → [CONCEPT.md#модерация-и-лимиты](CONCEPT.md#модерация-и-лимиты)
- Хранилище файлов (картинки, баннеры) → [STACK.md#хранилище-файлов](STACK.md#хранилище-файлов)
- Авторизация в ТМА (initData → сессия) → [STACK.md#авторизация](STACK.md#авторизация)
- Три интерфейса платформы (витрина/продавец/платформа) в одном приложении → [CONCEPT.md#интерфейсы-платформы](CONCEPT.md#интерфейсы-платформы)
- Фоновые задачи и очереди → [STACK.md#фоновые-задачи](STACK.md#фоновые-задачи)
- Хостинг, деплой и CI/CD → [STACK.md#хостинг-и-деплой](STACK.md#хостинг-и-деплой)
- Логирование и мониторинг → [STACK.md#логирование-и-мониторинг](STACK.md#логирование-и-мониторинг)
- Пайплайн запроса (routes → services → db) → [STACK.md#пайплайн-запроса](STACK.md#пайплайн-запроса)
- UX/UI-библиотека → [STACK.md#ui-кит](STACK.md#ui-кит)
- HTTP-клиент → [STACK.md#http-клиент](STACK.md#http-клиент)
- Тариф 1 без платёжного шлюза (оплата переводом, самозанятые) →
  [CONCEPT.md#платежи](CONCEPT.md#платежи)
- Коммуникация продавца и покупателя (диплинк в Telegram, без встроенного
  чата) → [CONCEPT.md#коммуникация-продавца-и-покупателя](CONCEPT.md#коммуникация-продавца-и-покупателя)
- Обзор альтернатив ЮKassa/СДЭК (не добавляются на старте, справочно) →
  [STACK.md#платежи-и-логистика-тарифы-2-3](STACK.md#платежи-и-логистика-тарифы-2-3)
- Бэкапы БД (nightly pg_dump в S3 + еженедельная проверка восстановления) →
  [STACK.md#бэкапы](STACK.md#бэкапы)
- Скоуп v1 (MVP) и критерий запуска (внутренняя + внешняя проверка):
  только Тариф 1, мануальная оплата/доставка, подписка через ЮKassa,
  платформенная админка сразу, без кастомизации и без канала жалоб →
  [CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp)
- Веха 2 дорожной карты (докер и локальное окружение): `docker-compose.yml`
  (postgres + `grammashop_test` + api + web), multi-stage `Dockerfile` на
  api/web, `docker compose up` отдаёт "hello world" — плейсхолдер-сервер
  в `apps/api` и статическая страница в `apps/web`, настоящий
  Fastify/Vite-скелет придёт по TDD в вехах 4-5
- Веха 3 дорожной карты (база данных): Drizzle ORM + drizzle-kit в
  `apps/api`, клиент подключения (`src/db/client.ts`), схема-заглушка
  `health_check`, первая миграция (`drizzle/0000_...sql`), миграция
  встроена в старт контейнера `api` — проверено с нуля через
  `docker compose down -v && docker compose up`
- Веха 4 дорожной карты (бэкенд): скелет Fastify (`app.ts`/`buildApp()`),
  пайплайн `routes → services → db` на примере `/health`
  (`routes/health.route.ts` → `services/health.service.ts` → таблица
  `health_check`), Zod-схема ответа в `packages/shared`
  (`healthResponseSchema`), первый тест по TDD — падающий
  `fastify.inject()`-тест до кода, зелёный после
  (`apps/api/src/routes/health.route.test.ts`, реальная `grammashop_test`,
  без мока Drizzle). Побочные находки по пути: `packages/shared` получил
  реальную `tsc`-сборку (был не пригоден к рантайм-импорту), общий
  `apps/api/src/env.ts` для резолвинга `.env` от файла, а не от `cwd`
  (чинит и старый `db/migrate.ts`), vitest `globalSetup` сам мигрирует
  `grammashop_test` перед прогоном тестов.
- Веха 5 дорожной карты (UX/UI): скелет Vite + React + TypeScript в
  `apps/web` (реальные `dev`/`build`, `index.html`-шаблон вместо статики),
  `react-router-dom` — три группы маршрутов (`src/router.tsx`,
  `src/routes/{storefront,seller,platform}`), `shadcn/ui` на Tailwind v4
  (`components.json`, `src/components/ui/button.tsx`), axios-клиент
  (`src/lib/api-client.ts`, `VITE_API_URL`), первый компонент по TDD
  (`HealthStatus` — падающий `@testing-library/react`-тест до кода,
  зелёный после). Проверено в докере с нуля (`docker compose down -v` →
  `up --build`) через headless-браузер (Playwright) против
  `localhost:5173`: все три маршрута рендерятся, статус `/health`
  подтягивается живым запросом к `apps/api`. Побочные находки: nginx без
  `try_files`-fallback отдавал 404 на `/seller`/`/platform` (client-side
  роутинг) — добавлен `apps/web/nginx.conf`; `apps/api` получил
  `@fastify/cors` (`app.ts`) — без него браузер с `:5173` не мог
  достучаться до `:3000`.
- Веха 6 дорожной карты (CI/CD): GitHub Actions
  (`.github/workflows/ci.yml`) — job `test` (postgres как
  service-контейнер, `pnpm -r test` на push/PR в `main`), job
  `build-and-push` (только на push) собирает `apps/api`/`apps/web` и
  пушит в GitHub Container Registry (`ghcr.io/seshmonke/grammashop-api`,
  `-web`, теги по SHA + `latest`) через встроенный `GITHUB_TOKEN`.
  Registry — GHCR, не Yandex Container Registry (см.
  [STACK.md#ci-cd](STACK.md#ci-cd)): не требует IAM-настройки в Yandex
  Cloud раньше времени. SSH-деплой на VM сознательно не написан — веха 7
  (сервер) ещё не существует. Проверено на реальном
  [прогоне](https://github.com/seshmonke/grammashop/actions/runs/29542419280):
  оба job'а зелёные, образы подтверждены через анонимный pull-токен
  ghcr.io.
- Веха 7 дорожной карты (деплой на сервер) закрыта целиком: VM, Caddy/TLS,
  S3, Sentry — из предыдущих спринтов; nightly `pg_dump` → S3 + weekly
  restore-check — Спринт 8, детали и проверка на бою в записях выше
  (раздел «Спринт 8»)
- Раздел «Логистика и доставка» сведён с тарифной таблицей — логистика
  явно привязана к тарифу (мануальная на Тарифе 1, СДЭК на 2-3), а не
  описана как свободный выбор продавца →
  [CONCEPT.md#логистика-и-доставка](CONCEPT.md#логистика-и-доставка)
- `DRAFT.md` почищен — устаревший раздел «Открытые вопросы» (все 5
  пунктов там были уже решены в CONCEPT.md) заменён на короткую отметку
  без открытых пунктов
- Доменная схема БД v1 согласована (8 таблиц; ПДн покупателя и позиции
  заказа — снапшотами; деньги в копейках; лимиты в сервис-слое; скидка
  через зачёркнутую цену; опциональные остатки с авто-учётом) →
  [STACK.md#доменная-схема-v1](STACK.md#доменная-схема-v1),
  [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы)
- Статусы заказа — минимальный набор подтверждён («новый → оплачен →
  выполнен / отменён», без «отправлен»/«получен»); отмена покупателем в
  статусе «новый»; авто-уведомление продавца о новом заказе через
  платформенного бота →
  [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы)
- Фронт-архитектура ТМА (вход по `start_param`, карта экранов трёх
  ролей, корзина на клиенте через context + reducer, TanStack Query для
  серверного состояния) → [STACK.md#роутинг](STACK.md#роутинг),
  [STACK.md#серверное-состояние-frontend](STACK.md#серверное-состояние-frontend)
- Дизайн-направление витрины: тема из Telegram `themeParams`, админки
  без декора — принципы актуальны; конкретное визуальное направление
  сменилось с тихого минимализма на Y2K, см. «Спринт 10» и
  [STACK.md#дизайн-направление](STACK.md#дизайн-направление)
- Telegram username продавца — обязательное требование регистрации
  (username в Telegram опционален; без него диплинк-канал связи с
  покупателем не существует) →
  [CONCEPT.md#коммуникация-продавца-и-покупателя](CONCEPT.md#коммуникация-продавца-и-покупателя)
- Дизайн-цикл прототипа — локально, не через прод (Vite hot reload +
  мок Telegram-API в браузере; Telegram-специфика — через dev-бота и
  туннель с локалки; прод — только финальная проверка) →
  [STACK.md#локальный-дизайн-цикл-и-dev-бот](STACK.md#локальный-дизайн-цикл-и-dev-бот)

## В работе

### Спринт 16: пайплайн пользовательских изображений

Цель: продавец может добавить фото к карточке товара — с валидацией и
ресайзом на бэке, покупатель видит его на витрине. Скоуп сознательно
ограничен пайплайном и отображением в существующей вёрстке (Спринт 9) —
токены Y2K и сетка каталога (кандидат «Перевод apps/web на Y2K
дизайн-система + сетка каталога» в «Очереди») переносятся в отдельный
Спринт 17, чтобы не смешивать инфраструктурную задачу (загрузка/
хранение файлов) с чисто визуальной в одном присесте. Приоритизировано
в диалоге 20.07.2026 — решает уже подтверждённую на бою проблему
(витрина «работает, но невзрачно», см. Спринт 9), а не гипотетическую:
альтернативный кандидат (пакетная Excel-заливка) отложен — сейчас нет
ни одного реального продавца с каталогом настолько большим, чтобы это
было болью. Задачи идут в порядке зависимости: спецификация → схема БД
→ бэк (TDD) → фронт → сквозная проверка.

- [x] ~~Спроектировать пайплайн и зафиксировать в
  [STACK.md#хранилище-файлов](STACK.md#хранилище-файлов): путь загрузки
  (прямой multipart через API — `@fastify/multipart`, не presigned URL,
  тот же принцип простоты, что и у остального API v1), валидация
  (разрешённые MIME-типы jpeg/png/webp, максимальный размер файла,
  защита от decompression bomb — ограничение на исходные пиксельные
  размеры до полного декодирования, не только на размер файла), ресайз
  (`sharp` — оригинал ограничен по большей стороне + отдельный
  thumbnail для списков), один слот фото на карточку (не на вариант, не
  галерея — минимальный достаточный скоуп v1, расширение до нескольких
  фото — отдельная будущая задача при реальной потребности), путь в S3
  (тот же бакет, что и у бэкапов, отдельный префикс
  `products/{sellerId}/{productId}/...`)~~ — сделано 20.07.2026 →
  [STACK.md#пайплайн-фото-товара-спринт-16](STACK.md#пайплайн-фото-товара-спринт-16).
  По пути найдено расхождение с планом: доменная схема, согласованная ещё
  в Спринте 9, уже содержит таблицу `product_images` (`s3_key` +
  позиция) — просто не использовалась. Вместо `imageUrl` на `products`
  (как было в исходном плане задачи 2) используется она — без новой
  миграции, лимит «1 фото на карточку» проверяется в сервис-слое, тем же
  приёмом, что и лимиты карточек/вариантов. Ключ thumbnail — производный
  от `s3_key` (суффикс `-thumb`), не отдельная колонка. Отдача на фронт —
  presigned GET-ссылки (бакет приватный), не голые URL.
- [x] ~~Схема: `imageUrl` (nullable text) в `products`, миграция~~ —
  отменено 20.07.2026, заменено решением выше: используется уже
  существующая `product_images`, миграция не нужна.
- [x] ~~Бэк, TDD: `POST /seller/products/:id/image` — приём файла,
  валидация, ресайз, загрузка в S3, запись URL; `DELETE
  /seller/products/:id/image` — удаление файла из S3 и очистка поля.
  Ownership тем же паттерном, что и у остального CRUD карточек (чужая
  карточка → 404, не 403)~~ — сделано 20.07.2026. `apps/api/src/images/`:
  `pipeline.ts` (чистая функция `processProductImage` — MIME-валидация,
  `sharp` с `limitInputPixels`, ресайз оригинала/thumbnail в webp; TDD
  без S3/БД — реальные синтетические PNG через сам `sharp`, не моки),
  `storage-keys.ts` (генерация ключа оригинала, вывод ключа thumbnail),
  `product-image-lookup.ts` (общие read-хелперы над `product_images` —
  presign, переиспользуются и продавцовской админкой, и витриной).
  `services/product-images.service.ts` — `setProductImage`/
  `deleteProductImage`, замена фото удаляет старые объекты S3, не копит
  строки. `s3/client.ts` дополнен `getPresignedImageUrl`
  (`@aws-sdk/s3-request-presigner`). Контракт — `productImageSchema`/
  `productImageUploadResponseSchema` в `packages/shared`, `image`
  добавлено в `sellerProductSchema` и `shopProductSchema` (видно и
  продавцу, и покупателю). RED-тесты первыми
  (`apps/api/src/routes/product-images.route.test.ts`, 7 кейсов — 404 на
  чужую карточку, 400 на неподдерживаемый MIME, загрузка/замена/удаление)
  — S3 замокан на границе клиента (`vi.mock("../s3/client.js")`, тот же
  приём, что и у `bot/client.js` в уведомлениях о заказах, — внешняя
  сеть, не БД), Postgres — реальная `grammashop_test`. Побочные находки:
  (1) `deleteProduct` каскадом сносил строку `product_images` на уровне
  FK, но не трогал объекты в S3 — фото продолжали бы занимать бакет
  после удаления карточки, исправлено явной очисткой перед удалением;
  (2) `S3Client` теперь конструируется при импорте основного сервера (а
  не только в отдельных скриптах verify/backup), пустой `S3_REGION`
  ронял старт сервера на любом запросе — добавлен фолбэк на реальный
  регион бакета (`ru-central1`) в `s3/client.ts`. Итого в api 106 тестов
  (было 92, +14: 7 route + 4 pipeline + 3 storage-keys).
- [x] ~~Фронт: загрузка/замена/удаление фото в `ProductForm.tsx` —
  превью перед сохранением, состояния загрузки/ошибки~~ — сделано
  20.07.2026. Доступно только для уже созданной карточки (`isEdit`) —
  загрузка фото отдельный запрос от остального CRUD и требует
  существующего `productId`, для новой карточки подсказка «фото можно
  добавить после сохранения». `useUploadProductImage`/
  `useDeleteProductImage` (`seller/useSellerProducts.ts`, `FormData`,
  тот же TanStack Query приём, что и у остальных мутаций). Локальный
  превью (`URL.createObjectURL`) показывается сразу при выборе файла, до
  ответа сервера; после успешной загрузки инвалидация запроса подтягивает
  presigned-ссылку с бэка. Ошибка загрузки/удаления — отдельным текстом
  под блоком фото, не общей ошибкой формы.
- [x] ~~Фронт: отображение фото на витрине — список карточек на
  `StorefrontHome.tsx` и `ProductDetail.tsx`, в текущей вертикальной
  вёрстке (без сетки — это Спринт 17); плейсхолдер на дизайн-токенах
  для карточек без фото~~ — сделано 20.07.2026. `ProductCard.tsx` —
  thumbnail 64×64 слева от текста (было: чисто текстовая карточка),
  `ProductDetail.tsx` — квадратное фото по ширине экрана над названием.
  Плейсхолдер «Нет фото» на текущих `tg-*` токенах (не Y2K — тот перевод
  вынесен в Спринт 17, см. «Очередь»). `pricing.test.ts` донастроен под
  новое обязательное поле `image` в контракте.
- [x] ~~Сквозная проверка на бою: фото, загруженное через продавцовскую
  админку, видно на витрине с телефона~~ — подтверждено 20.07.2026.
  Задеплоено в прод пушем `main` (коммит `83afd13`, CI
  test→build→deploy зелёный целиком). С телефона через
  `@grammashopbot` фото загружено через продавцовскую админку и видно на
  витрине (`?startapp=1`) — реальный S3 (Yandex Object Storage,
  presigned-ссылки), реальный `initData`.

**Спринт 16 закрыт** — продавец загружает/заменяет/удаляет фото карточки
через собственную админку, покупатель видит его на витрине и в карточке
товара; проверено на бою с реальным S3 и Telegram.

### Спринт 17: Y2K-дизайн витрины + сетка каталога

Цель: витрина покупателя (не админки — токены и сетка касаются только
её) переведена на дизайн-систему из Спринта 10 (Y2K — хром/магента/ice,
Unbounded), карточки идут флюидной сеткой вместо вертикального списка.
Продолжает Спринт 16 (фото теперь есть — без них сетка была бы
половинчатым результатом). Согласовано в диалоге 20.07.2026 (см.
[STACK.md#сетка-каталога--кросс-платформенная-вёрстка](STACK.md#сетка-каталога--кросс-платформенная-вёрстка)
по решению о флюидной сетке). Три подзадачи одним присестом — токены
сами по себе не дают законченного результата, сетка без токенов
выглядела бы ещё страньше. Растровый favicon и аватарка бота — не в
этот спринт (аватарка — ручное действие пользователя в BotFather, не
код), отдельные строки в «Очереди».

- [x] ~~Токены: `apps/web/src/index.css` на палитру/типографику Y2K
  (см. [docs/design/DESIGN_SYSTEM.md](design/DESIGN_SYSTEM.md)) —
  акцент поверх темы Telegram `themeParams`, не замена; админки не
  трогать (остаются на текущей плоской системе)~~ — сделано 20.07.2026.
  Палитра (`--void*`/`--chrome*`/`--magenta*`/`--ice`) и Unbounded
  (Google Fonts, `apps/web/index.html`, только `wght@500..900` —
  дисплейные моменты, не тело интерфейса) добавлены в `:root` рядом с
  существующими `--tg-*`/shadcn-токенами, не поверх них — обе админки их
  не подключают и не задеты. Утилити-классы `.y2k-heading`/`.y2k-label`
  (`font-variation-settings`), `.y2k-price-glow`/`.y2k-cta-glow` (magenta
  text-shadow/box-shadow, см. DESIGN_SYSTEM.md#приёмы — glow только на
  коммерческих/CTA-элементах), `.y2k-scanlines` (едва заметная фактура
  фона). **Правка по ходу визуальной сверки**: чистый `--magenta`
  текстом поверх светлой `--tg-surface` даёт ~3.4:1 контраста — ниже
  WCAG AA (4.5:1) для обычного текста, обнаружено скриншотом на светлой
  теме, не на глаз пропущено. Т.к. Telegram-тема (light/dark) и тема ОС —
  разные вещи, полагаться на `prefers-color-scheme` как признак темы
  Telegram нельзя; вместо этого — токен
  `--magenta-on-theme: color-mix(in srgb, var(--magenta) 65%, var(--tg-text) 35%)`
  — подмешивает гарантированно контрастный (это забота Telegram, не
  наша) `--tg-text`, автоматически читаем в любой теме без ветвления.
  Применяется только к тексту цены поверх `--tg-surface`/`--tg-bg`;
  бейджи/CTA на собственной заливке `--magenta` (белый текст на ней) не
  затронуты — там контраст самодостаточен. По той же причине `--ice`
  текстом (было опробовано на кнопках степпера количества и ссылке
  «Назад» в `ProductDetail.tsx`) — тоже проваливал контраст на светлой
  теме (~1.6:1, из Y2K-палитры откалиброван под тёмные брендовые
  поверхности, не под переменный `--tg-surface`) — откачено обратно на
  `--tg-text`/`--tg-link` в этих двух местах.
- [x] ~~Сетка каталога: `StorefrontHome.tsx` — флюидная CSS Grid
  (`repeat(auto-fill, minmax(--card-min, 1fr))`), `max-width`
  контейнера ограничивает плотность на широких экранах; `--card-min` и
  `max-width` подобрать Playwright-сверкой на сетке вьюпортов
  320-1920px (не на глаз, тот же приём, что в Спринте 15), минимум 2
  колонки на самом узком поддерживаемом экране~~ — сделано 20.07.2026,
  `--card-min: 136px`, `--catalog-max-width: 900px`. Проверено скриптом
  на живом dev-каталоге (13 карточек, не 3 как у seed-продавца — иначе
  верхняя граница плотности не проверяется, карточек не хватает на
  полный ряд): 2 колонки на 320-428px, плавный рост через 4/5 на
  600-768px, плато на 6 колонках с 900px и до 1920px, горизонтального
  переполнения нет ни на одном шаге сетки.
- [x] ~~`ProductCard.tsx`: image-first раскладка (фото `aspect-square`
  сверху, имя/цена/скидка под ним), компактная кнопка «В корзину»
  (иконка). Плейсхолдер без фото — приглушённый (`opacity` ~0.35-0.4,
  без glow) `logo.svg` по центру плитки на `--void-2`~~ — сделано
  20.07.2026. Компактная кнопка — иконка `Plus` (lucide-react) в
  magenta-круге с glow поверх фото (правый нижний угол), при товаре уже
  в корзине показывает количество вместо иконки; skidка — magenta-бейдж
  с процентом под ценой (для товара с единственным вариантом — с
  несколькими выбор всё равно требует перехода в карточку, тот же
  принцип, что и у самой кнопки «В корзину»). Проверено скриншотами:
  плейсхолдер и реальное фото (через перехват ответа `/shop/:id` в
  Playwright — S3 не сконфигурирован на этой машине для реального
  аплоада, сама загрузка в S3 уже проверена на бою в Спринте 16, здесь
  проверялась только вёрстка вокруг `<img>`).
- [x] ~~`ProductDetail.tsx`: список вариантов остаётся (устоявшийся PDP-
  паттерн), получает токены Y2K и фото сверху при наличии~~ — сделано
  20.07.2026. Цена и скидка — те же magenta/glow токены, что и в карточке
  каталога; плейсхолдер фото — тот же приём `--void-2` + приглушённый
  `logo.svg`, что и в `ProductCard.tsx` (не отдельная реализация).
- [x] ~~Сквозная проверка на бою: витрина на телефоне и (если доступно)
  на десктопном Telegram-клиенте — сетка не разъезжается, тема
  Telegram (light/dark) по-прежнему подхватывается поверх
  Y2K-акцента~~ — подтверждено с телефона 20.07.2026 (после `git push
  origin main`, коммит `dccc825`, CI test→build→deploy зелёный):
  `t.me/grammashopbot?startapp=1`, сетка каталога держится, Y2K-акцент
  (магента/хром/лёд, шрифт Unbounded) применился на витрине и в карточке
  товара поверх темы Telegram.

**Спринт 17 закрыт** — витрина покупателя переведена на Y2K-дизайн
(токены, флюидная сетка каталога, image-first карточка), проверено на
бою с телефона. Обе админки и корзина/чекаут остаются на прежней
нейтральной системе — не входили в скоуп, см.
[docs/design/DESIGN_SYSTEM.md#реализация](design/DESIGN_SYSTEM.md#реализация).

### Спринт 18: пакетная заливка товаров через Excel-шаблон

Цель: продавец с уже оцифрованным каталогом скачивает шаблон, заполняет
его в Excel/Google Sheets и одним файлом заводит карточки с вариантами
— без формы `ProductForm.tsx` по одной штуке. Выбрано пользователем
20.07.2026 из очереди (см. историю решения по трём открытым вопросам —
формат/библиотека/партиальный импорт — в
[STACK.md#пакетная-заливка-каталога-спринт-18](STACK.md#пакетная-заливка-каталога-спринт-18)).
Задачи идут в порядке зависимости: формат шаблона → бэк (TDD) → фронт →
сквозная проверка.

- [x] ~~Статический файл-шаблон (`.xlsx` в `apps/web/public/`, тот же
  паттерн, что `offer.html`) — колонки по
  `createProductRequestSchema`: название карточки, описание, вариант,
  цена (руб.), старая цена (руб., опционально), остаток (опционально);
  название/описание повторяются на каждой строке варианта карточки (см.
  STACK.md — устойчивость к сортировке/фильтрации в Excel). Пара строк
  примера в шаблоне, не только заголовки — нагляднее для нетехнического
  продавца~~ — сделано 20.07.2026, `apps/web/public/products-template.xlsx`
  (сгенерирован разовым скриптом через `exceljs`, скрипт не сохранён —
  тот же прецедент, что и у `og-image.png` в Спринте 15). Инструкция по
  формату — не отдельной строкой таблицы (иначе парсер принял бы её за
  невалидную строку данных), а Excel-примечанием (note) на заголовке
  колонки «Название». Проверено обратным разбором через
  `parseImportWorkbook` — 2 карточки, 0 ошибок на нетронутом шаблоне.
- [x] ~~Бэк, TDD: парсинг `.xlsx` (`exceljs`) → группировка строк по
  названию карточки → валидация той же Zod-схемой, что и ручной API →
  создание через существующий `services/products.service.ts`
  (`createProduct`/`addVariant`, лимиты 30/10 проверяются там же, не
  дублируются в парсере). `POST /seller/products/import` — партиальный
  импорт: валидные карточки создаются, невалидные строки идут в
  построчный отчёт ответа (`{row, error}`), не блокируют файл целиком~~ —
  сделано 20.07.2026. Чистая функция `products-import/parse-workbook.ts`
  (TDD без БД, 7 тестов: группировка по названию, обязательные/опциональные
  поля, пустые строки молча пропускаются, одна невалидная строка не рушит
  остальные варианты той же карточки) → `services/products-import.service.ts`
  (`importProducts` — каждая группа проверяется `createProductRequestSchema`,
  затем `createProduct`, лимит карточек и >10 вариантов дают построчную
  ошибку на все затронутые строки, не 500) → `POST /seller/products/import`
  в `products.route.ts` (тот же паттерн, что у `/image`: `request.file()`,
  проверка MIME на `.xlsx`). RED-тесты первыми
  (`routes/products-import.route.test.ts`, 7 кейсов: 401/403/400-MIME,
  успешный импорт с копейками, партиальный импорт, >10 вариантов, лимит 30
  карточек) — реальная `grammashop_test`, без моков. Контракт
  `productImportResponseSchema` в `packages/shared`. Итого в api 120 тестов
  (было 106, +14).
- [x] ~~Фронт: экран импорта в продавцовской админке — ссылка на скачивание
  шаблона, загрузка заполненного файла, отображение отчёта (сколько
  карточек создано, список ошибок по строкам)~~ — сделано 20.07.2026.
  Не отдельный экран, а панель на `SellerHome.tsx` (та же логика, что у
  решения не заводить отдельный роут под однократное действие) —
  ссылка на шаблон, скрытый `<input type=file accept=".xlsx">` за кнопкой
  (тот же приём, что у загрузки фото в `ProductForm.tsx`), `useImportProducts`
  (`seller/useSellerProducts.ts`, TanStack Query mutation, инвалидирует
  список товаров). Отчёт не бросает на непустой `errors` — это не сетевая
  ошибка запроса, показывается вместе с `createdCount`. Проверено вживую
  headless-браузером (Playwright, dev-сервер + локальный Postgres,
  `AUTH_DEV_MODE`): загрузка чистого шаблона — 2 карточки созданы, 0 ошибок;
  загрузка файла с намеренно невалидными строками (пустой вариант,
  отрицательная цена) — 1 карточка создана, 2 ошибки показаны построчно
  («строка 3: не заполнено название варианта», «строка 4: цена должна
  быть больше 0»), созданная карточка сразу видна в списке.
- [ ] Сквозная проверка на бою: реальный `.xlsx`-файл с несколькими
  карточками (включая одну заведомо невалидную строку) загружен через
  продавцовскую админку на проде — валидные карточки видны на витрине,
  ошибка по невалидной строке показана явно.

**Инцидент при деплое, найден и исправлен 20.07.2026**: после пуша
`git push origin main` панель импорта не появилась на проде, хотя CI
(`test→build→deploy`) отчитался зелёным. Причина — гонка между двумя
CI-ранами: коммит `2a117df` (только доки, «Спринт 18 согласован») и
следом `7f20f40` (код) породили два параллельных рана; оба
`build-and-push` пушат один и тот же мутируемый тег `:latest`, ран
`2a117df` финишировал на ~2 секунды позже и затёр `:latest` версией
без кода Спринта 18 — `deploy` уже нового коммита стянул устаревший
образ. Диагностировано SSH на VM: `git log` на сервере показывал
верный коммит, но собранный JS-бандл не содержал строк фичи; локальная
пересборка того же Dockerfile с тем же кодом дала бандл с фичей —
разница была именно в том, какой образ подхватил `:latest`, SHA-теги в
GHCR были корректны у обоих. Восстановление — на VM образы
перетегированы с точного SHA `7f20f40...` обратно в `latest`,
`docker compose up -d`, проверено внешним `curl`: бандл содержит
`products/import`, `products-template.xlsx` отдаётся 200. Профилактика —
`concurrency`-группа в `.github/workflows/ci.yml` (`cancel-in-progress:
true` по ветке, см.
[STACK.md#ci-cd](STACK.md#ci-cd)) — более старый ран теперь отменяется
целиком при новом пуше, до registry доезжает только последний коммит.

## Очередь

- **Возврат товара покупателем (закон о защите прав потребителей)** —
  заведено 19.07.2026 по запросу пользователя, требование явно
  сформулировано: минимальная ответственность платформы. Сейчас механика
  возврата не спроектирована вообще — ни в
  [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы), ни в схеме
  БД. Отправная точка та же, что уже принята для оплаты: платформа не
  сторона сделки между продавцом и покупателем — деньги идут напрямую,
  без эскроу, без агентского договора (см.
  [CONCEPT.md#юридическая-форма-отношений-с-продавцом](CONCEPT.md#юридическая-форма-отношений-с-продавцом)).
  Разбор (рабочий черновик, требует проверки юристом, как и весь юрпакет
  152-ФЗ — не финал):
  1. **Юридическая рамка** — дистанционная торговля (продажа через
     мессенджер подпадает под правила дистанционной продажи, ст. 26.1
     Закона о защите прав потребителей + Постановление Правительства
     №2463): покупатель вправе отказаться от товара **надлежащего**
     качества в течение 7 дней после получения без объяснения причин
     (кроме отдельных исключений — например, товар с индивидуально
     определёнными свойствами, если явно указан этот признак при
     покупке), и вернуть товар **ненадлежащего** качества в течение
     гарантийного/разумного срока. Это обязанность продавца как стороны
     сделки — платформа юридически не обязана организовывать возврат,
     но должна явно закрепить это разделение в оферте (текст уже несёт
     общий принцип «платформа — информационный посредник», см.
     `apps/web/public/offer.html`, п.2 — пункт про возврат нужно
     добавить туда же явно, не полагаться на общую формулировку).
  2. **Что платформа обязана дать технически** (в отличие от
     юридической обязанности — вот это уже наш код): вероятно (а) канал
     связи для запроса возврата — тот же паттерн, что и вся
     коммуникация продавца/покупателя, диплинк в Telegram, не встроенный
     чат/арбитраж (см.
     [CONCEPT.md#коммуникация-продавца-и-покупателя](CONCEPT.md#коммуникация-продавца-и-покупателя))
     — платформа не обязана быть посредником в споре; (б) решить, нужен
     ли отдельный статус заказа под возврат или это вне статусной модели
     вообще (сейчас «новый→оплачен→выполнен/отменён», «отменён» —
     терминальный без отката, см.
     [CONCEPT.md#каталог-и-заказы](CONCEPT.md#каталог-и-заказы) — возврат
     это про уже выполненный заказ, другая ветка, не переход из
     существующей статусной модели механически).
  3. **Текст в оферте/публичной странице** — явно прописать право
     покупателя на возврат по закону и то, что весь процесс (кто платит
     за обратную доставку, куда физически возвращать товар, кто и когда
     возвращает деньги) — предмет прямой договорённости продавца и
     покупателя, платформа не участвует и не гарантирует исход. Тот же
     юрпакет, что уже в очереди ниже («Юридический пакет»), должен
     закрыть и этот пункт — не отдельный заказ у юриста.
  **Рекомендация по минимальному скоупу v1/v2**: платформа не
  автоматизирует ни возврат денег, ни логистику назад — только даёт
  покупателю канал связаться с продавцом (уже есть, диплинк в личку с
  экрана заказа) и честно фиксирует в оферте право на возврат по закону.
  Главный компромисс — простота против контроля: минимум кода и
  минимум ответственности платформы, но и минимум автоматизации для
  продавца/покупателя (по аналогии с уже принятым решением по доставке
  на Тарифе 1 — вне ТМА, по прямой договорённости сторон).
- **Прод-VM не достаёт до Telegram API — уведомления продавцу не
  доходят** — обнаружено 20.07.2026 при сквозной проверке чекаута
  Спринта 12: заказ оформляется (`POST /shop/:sellerId/orders` → 201),
  задача исправно встаёт в очередь pg-boss (`order-notification`,
  воркер её подхватывает), но `sendMessage` через grammY падает
  `ETIMEDOUT`. Диагностика с самой VM (`89.169.191.173`, и с хоста, и
  изнутри контейнера `api`): `api.telegram.org`, `t.me`, `telegram.org`
  — TCP-соединение не устанавливается вообще (не DNS, не HTTP-уровень),
  при этом обычный интернет с той же VM работает (Google/GitHub/Яндекс
  — 200). Похоже на блокировку Telegram на уровне сети/провайдера для
  российских серверов (Yandex Cloud, `ru-central1`) — известная история
  с блокировками Telegram в РФ, от которых клиентские приложения уходят
  через встроенные обходы (MTProto-обфускация и т.п.), а прямой
  HTTPS-запрос с сервера — нет. Важное следствие: `/auth` при этом
  работает — проверка `initData` идёт локальным HMAC, без сети до
  Telegram, поэтому это не было замечено раньше; любой **другой** будущий
  функционал, требующий исходящих запросов к Telegram API с сервера
  (не только уведомления о заказах), упрётся в то же самое. Решение
  отложено — сначала перепроверить, не временная ли блокировка (curl
  с VM через несколько часов/дней), если стабильно — рассмотреть
  прокси/туннель для исходящих запросов grammY (библиотека поддерживает
  кастомный fetch) и завести отдельным пунктом с выбором конкретного
  прокси-сервиса.
- **Механика уведомлений продавцу через бота — продумать отдельно от
  сетевой проблемы выше** — вопрос поднят пользователем 20.07.2026 при
  проверке того же чекаута: единственный продавец в проде сейчас — это
  и есть владелец платформы (`syzrp`), сообщения технически шли в его
  собственный чат с `@grammashopbot`; для произвольного стороннего
  продавца это не было проверено как общий механизм. Разбор:
  - Ограничение Telegram Bot API — бот не может написать пользователю
    первым, только если тот сам хотя бы раз открыл с ботом диалог/Mini
    App. Поскольку регистрация продавца в любом случае идёт через
    `@grammashopbot`, условие выполняется самим фактом регистрации —
    специально ничего заводить не нужно, но это предположение явно
    зафиксировано как требование к будущей форме регистрации (см.
    продуктовую карту, пункт 3 «Регистрация и подписка продавца»), а не
    оставлено молчаливым.
  - Кейс «продавец заблокировал бота после регистрации» —
    `sendMessage` вернёт 403, ловится тем же путём, что и сетевые сбои
    (job уходит в `failed` в pg-boss), покупательский флоу не падает.
  - **Пробел без решения**: провал `sendOrderNotification` сейчас нигде
    не виден платформенному админу — только вручную через SQL по
    `pgboss.job` (как и делалось при разборе этой самой находки).
    Нет ни Sentry-репорта из воркера (в отличие от `enqueueOrderNotification`,
    у самого воркера `registerOrderNotificationWorker` try/catch с
    `Sentry.captureException` не стоит — ошибка просто утекает в
    pg-boss), ни какого-либо алертинга/дашборда. На масштабе 1-10
    продавцов это терпимо (можно проверить вручную по жалобе продавца
    «не пришло уведомление»), но не решение, а временная терпимость —
    зафиксировать как отдельный технический долг, не путать с самой
    доставкой сообщения.
- **Снести демо-продавца #1 с прода** перед регистрацией первого
  реального продавца — заведён 19.07.2026 для сквозной проверки Спринта 9
  (`SEED_ALLOW_PROD=1`, telegram_id владельца платформы), в проде не
  предполагался как постоянные данные. Удаление — вручную по SSH
  (`delete from sellers where id = 1` с каскадом, или повторный прогон
  seed-скрипта с другим `DEV_SELLER_TELEGRAM_ID` поверх). Не блокер, но
  не забыть до v1. **Новая зависимость (20.07.2026, актуальна и после
  реализации в Спринте 15):** публичный лендинг (`landing.html`)
  ссылается на витрину демо-продавца как на честный «живой пример» —
  снос откладывается как минимум до появления первого реального
  продавца или замены примера на лендинге, иначе ссылка с лендинга
  ведёт в пустоту.

- **Выбор адреса доставки на карте в чекауте** — сейчас `buyerAddress`
  вводится только текстом (см. CONCEPT.md#каталог-и-заказы). Предложено
  20.07.2026, отложено до отдельного обсуждения — открытые вопросы,
  которые нужно решить до кода: (1) провайдер карт — по духу
  STACK.md, вероятно Яндекс Карты (российская юрисдикция, тот же
  принцип, что у S3/ЮKassa/СДЭК), но нужен платный API-ключ и явное
  решение; (2) хранить ли координаты отдельно от текста в схеме заказа
  (сейчас `buyerAddress` — просто `text`) или только геокодировать для
  UI, не сохраняя; (3) ценность ниже на Тарифе 1 (доставка вручную вне
  платформы, координаты продавцу не нужны для работы) — выше на
  Тарифах 2-3 при подключении СДЭК, так что имеет смысл проектировать
  вместе с той интеграцией, а не раньше.
- **Растровый favicon + аватарка бота** — вынесено из Спринта 17
  (`apps/web` на Y2K, см. «В работе»): `.ico`/PNG-набор из `logo.svg`
  (SVG-фавикон уже подключён в `index.html`, растрового набора для
  старых контекстов нет — см.
  [docs/design/DESIGN_SYSTEM.md#не-реализовано](design/DESIGN_SYSTEM.md#не-реализовано)),
  аватарка `@grammashopbot` в BotFather — **ручное действие
  пользователя**, не код.
- **Онбординг мерчанта ЮKassa — уточнён и расписан 20.07.2026** (блокер
  п.3 продуктового бэклога). Разобрано подробно, т.к. модерация всё ещё
  не пройдена, а по ходу разбора всплыла ошибка в CONCEPT.md (см. правку
  там же, «Оплата подписки продавцом»): реальная последовательность шагов
  такая, и она длиннее, чем считалось раньше:
  1. **Зарегистрировать ИП владельцу платформы** — этого шага раньше не
     было в очереди вовсе (ошибочно считалось, что ИП уже есть).
     Госпошлина 800 ₽, **не взимается при подаче в электронном виде**
     (ФНС/Госуслуги/банк с ЭЦП) — практически бесплатно. Обработка — до
     3 рабочих дней. Это первый шаг, раньше которого ЮKassa-регистрацию
     не начать (нужны ИНН/ОГРНИП).
  2. **Регистрация мерчант-аккаунта в ЮKassa**: личный кабинет →
     подтверждение телефона → тип «ИП» → данные по ИНН/ОГРНИП → адрес
     сайта (`grammashop.online`), категория услуг, модель работы
     («подписка»). Модерация — от нескольких часов до 2-3 дней, **но
     проверяет не только документы ИП, а и сам сайт**: наличие публичной
     оферты, политики обработки ПДн, контактов. Сейчас на
     `grammashop.online` нет ни одной публичной страницы (весь фронт —
     Mini App за Telegram-авторизацией) — без публикации оферты/политики
     модерация не пройдёт технически, это тот же блокер, что и пункт про
     152-ФЗ ниже, не отдельный.
  3. **Автоплатежи (рекуррент)** — отдельный шаг после базового
     одобрения, самостоятельно не включается: нужно писать персональному
     менеджеру ЮKassa, приложив (а) скриншот/описание, как продавец
     отвязывает карту от автосписаний, (б) оценку оборота по рекурренту.
     Оферта должна явно называть сумму и период списания. Заодно
     закрывается вопрос механики Тарифа за 1 ₽ (рекуррент 1 ₽/мес vs
     верификация карты — см. пункт из ревью ниже) — уточняется у того же
     менеджера.

  Источники: [Регистрация в ЮKassa](https://yookassa.ru/docs/support/merchant/payments/implement/start),
  [Автоплатежи в ЮKassa](https://yookassa.ru/regulyarnye-platezhi/),
  [госпошлина за регистрацию ИП в 2026](https://www.buxprofi.ru/information/gosposhlina-za-registraciju-IP).
- **Юридический пакет (оферта + 152-ФЗ) — теперь общий блокер для двух
  вещей**, не только для запуска v1: и для модерации ЮKassa (см. выше,
  сайту нужна публичная оферта/политика), и для самого приёма ПДн
  покупателей в чекауте (решение 17.07.2026). Состав пакета зафиксирован
  в [CONCEPT.md#персональные-данные-152-фз](CONCEPT.md#персональные-данные-152-фз)
  (см. также [reviews/2026-07-16.md](reviews/2026-07-16.md), концепция
  п. 5): политика обработки ПДн, согласия, уведомление РКН, внутренние
  документы ИП, плюс сама оферта (модель уже зафиксирована в CONCEPT.md,
  нужен готовый юридический текст).
  **Стоимость и процесс (справочно, рынок на 20.07.2026):** разброс
  большой — только оферта под IT-подписку от ~13 000 ₽ (разработка) до
  ~2 000-10 000 ₽ (адаптация готового шаблона под IT-компанию); полный
  пакет 152-ФЗ под ключ (политика + согласия + уведомление РКН +
  внутренние документы) — от ~5 000 ₽ до 50 000+ ₽ в зависимости от
  объёма и того, делает ли юрист аудит с нуля или адаптирует шаблон;
  срок — от пары дней (шаблон) до ~2 недель (юрист с нуля). Практический
  путь — начать с юриста/сервиса, специализирующегося на IT/интернет-проектах
  (не универсального), т.к. модель платформы (маркетплейс-посредник,
  агентская схема с продавцами) нетиповая для готовых шаблонов.
  Источники: [составление оферты — цены](https://www.dvitex.ru/docs/dogovory/podgotovka_oferty/),
  [IT-юрист, прайс](https://profi.ru/buhgaltery_i_yuristy/it-yurist/price/),
  [пакет 152-ФЗ под ключ](https://www.consult-gp.ru/razrabotka_paketa_dokumentov_po_zashchite_personalnykh_dannykh.php).
  **Публичная страница на `grammashop.online` (оферта/политика/контакты)
  свёрстана 20.07.2026 заранее**, с рабочим черновиком текста (не финал —
  явно помечено на странице), чтобы не ждать юриста для самого кода —
  см. отдельную запись ниже, текст заменяется на финальный после
  согласования.
- Пересчитать цены тарифов (399 ₽ / 999 ₽) после расчёта юнит-экономики
  платформы
- Кнопка «Открыть магазин» через платформенного бота-админа канала —
  вне v1 (см.
  [CONCEPT.md#кнопка-открытия-магазина](CONCEPT.md#кнопка-открытия-магазина)),
  доступна по умолчанию на любом тарифе. Требует Bot API-доступа к
  чужим каналам (права администратора платформенного бота, хранение
  привязки «пост ↔ канал продавца») — отдельный кусок работы, не просто
  UI поверх уже спроектированного пайплайна.

### Из ревью 19.07.2026 (см. [reviews/2026-07-19.md](reviews/2026-07-19.md))

Уже стоящее в очереди не дублируется: онбординг мерчанта ЮKassa и
юридический пакет 152-ФЗ (два внешних блокера v1 из резюме ревью),
staging, снос демо-продавца #1 — см. пункты выше и ниже.

- **Скрытие витрины при `suspended`-подписке** — `shop.service` сейчас
  отдаёт каталог независимо от подписки (осознанно, но только
  комментарием в коде). Явный пункт с тестом — в задачу «Регистрация и
  подписка продавца» (п. 3 продуктовой карты), чтобы обещание из
  [CONCEPT.md#оплата-подписки-продавцом](CONCEPT.md#оплата-подписки-продавцом)
  не потерялось между биллингом и витриной (ревью, код п. 3).
  Seed-продавец (`db/seed.ts`) — основа проверки витрины через dev-бота:
  при реализации скрытия он должен оставаться видимым (подписка
  `active`, `paid_until` в будущем — сейчас +30 дней; если проверка
  завяжется на `paid_until`, протухший seed лечится перезапуском
  `db:seed`, зафиксировать это в подсказке скрипта).
- **Сценарий удаления данных продавца (152-ФЗ)** — решить и
  зафиксировать в
  [CONCEPT.md#персональные-данные-152-фз](CONCEPT.md#персональные-данные-152-фз):
  обезличивание записи `sellers` vs каскад, судьба заказов
  (`orders.seller_id` сейчас без `ON DELETE` — удаление упадёт по FK).
  У покупателя сценарий определён, у продавца — нет; закрыть до
  появления реальных продавцов (ревью, код п. 4).
- **Runtime-образ `apps/api` тащит весь монорепозиторий** —
  dev-зависимости, исходники, `apps/web`. Ужать через `pnpm deploy
  --prod`/отдельный prod-install (`/docker:optimize`). Не срочно
  (ревью, код п. 5). Ограничения: тот же Dockerfile собирается дев-циклом
  (`scripts/dev-tunnel.sh` → `docker compose up --build api`), поэтому в
  образе должны остаться `dist/db/{seed,backup,restore-check}.js`
  (запускаются через `docker compose exec`) и `postgresql16-client`
  (`pg_dump`/`psql`); после ужатия прогнать и дев-цикл с телефона, и
  `db:seed` в контейнере, не только прод.
- **Шифрование бэкапов в S3** — дампы содержат все ПДн платформы (и в
  будущем токены карт), доступ равен ключу сервисного аккаунта в `.env`
  на том же VM. Взвесить SSE бакета или gpg перед загрузкой — до
  появления реальных покупательских данных (ревью, код п. 6).
- **CORS-плагин не регистрировать вне dev** — в проде same-origin через
  Caddy, а `origin: localhost:5173` в `app.ts` безвреден, но лишний
  (ревью, код п. 7). Гейтить по `NODE_ENV !== "production"`, не убирать
  целиком: локальный цикл «браузер на `:5173` (docker-nginx) → api на
  `:3000`» — кросс-ориджин и без CORS сломается; туннельный цикл
  dev-бота CORS не использует (same-origin через Vite-прокси `/api`),
  но проверить после правки оба пути.
- **Убрать `PAYMENT_KEYS_MASTER_KEY` из `.env.example`** — переменная
  «про запас» для фичи v2 без кода и без решённой схемы хранения;
  контекст хранит задача «Схема хранения платёжных ключей продавцов»
  ниже (ревью, «Документация и процесс»).

### Из ревью 16.07.2026 (см. [reviews/2026-07-16.md](reviews/2026-07-16.md))

Концепция и продукт:

- **Проверить руками онбординг ЮKassa как тестовый продавец** (какой URL
  магазина указывать на модерации, требования к статусу самозанятого/ИП)
  — переносится на **v2**, не v1. Это онбординг продавца в его
  *собственном* ЮKassa-аккаунте для приёма оплаты от покупателей (Тарифы
  2-3), а не платёжная механика v1: на Тарифе 1 оплата покупателем —
  мануальный перевод, без платёжного шлюза (см.
  [CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp)). ЮKassa в v1 нужна
  только платформе — для приёма собственной подписки с продавца (см.
  [CONCEPT.md#оплата-подписки-продавцом](CONCEPT.md#оплата-подписки-продавцом)),
  это отдельная и более простая интеграция без модерации магазина
  продавца. Требования СДЭК к выдаче API-ключей — туда же, в v2
  (концепция п. 2).
- **Уточнить механику Тарифа за 1 ₽** по документации ЮKassa — для
  собственного мерчант-аккаунта платформы, не продавца: рекуррентно
  списывать 1 ₽/мес vs одна верификация карты при привязке (списание/холд)
  без ежемесячных микросписаний — фильтр тот же, движений денег и рисков с
  эквайрингом/банками меньше. Актуально для v1 (концепция п. 4).

Стек и эксплуатация:

- **Схема хранения платёжных ключей продавцов** — зафиксировать в
  `STACK.md` до кода интеграций: шифрование ключей в БД (мастер-ключ не в
  той же БД и не рядом в compose), write-only поле (ввести можно, прочитать
  нельзя — только заменить), доступ к расшифровке — только у сервиса,
  вызывающего ЮKassa/СДЭК (концепция п. 3). Актуально для v2 — в v1 у
  продавцов нет собственных платёжных/логистических ключей (см.
  [CONCEPT.md#скоуп-v1-mvp](CONCEPT.md#скоуп-v1-mvp)); ключи ЮKassa самой
  платформы для приёма подписки — отдельный, более простой случай (не
  per-продавец).
- **Механизм отзыва сессии** — заблокированный продавец с живым JWT не
  должен работать до истечения токена; проверка статуса по БД на
  чувствительных операциях. Записать в `STACK.md`, раздел «Авторизация»
  (концепция п. 3). **Решить в рамках Спринта 14** (платформенная
  админка) — раньше это была абстрактная задача без потребителя, теперь
  admin получает реальную кнопку «заблокировать», и вопрос «насколько
  быстро блокировка вступает в силу» встаёт предметно. Развилка: (а)
  оставить как есть — 1ч TTL JWT уже является естественной верхней
  границей, ничего не делать; (б) добавить проверку статуса продавца по
  БД в `requireSellerId`/`fastify.authenticate` на чувствительных
  операциях — лишний DB-хит на каждый запрос ради более быстрого эффекта
  блокировки. Решение — при реализации Спринта 14, не раньше.
- ~~Пайплайн пользовательских изображений~~ — взят в работу, см.
  «В работе», Спринт 16.
- **Rate limiting и защита API** — заложить `@fastify/rate-limit` и лимиты
  на создание заказов в модуле авторизации и вебхуках ЮKassa/СДЭК сразу,
  а не после первого абьюза (стек п. 4).
- **Идемпотентность вебхуков ЮKassa** — повторная доставка события не
  должна дважды продлевать подписку; ляжет в pg-boss, но зафиксировать
  явно. Актуально для v1 (подписка продавца платформе идёт через ЮKassa
  с первого продавца) — часть про оплату заказа покупателем не
  применяется в v1, там нет платёжного шлюза (стек п. 5).
- **Staging** — зафиксировать деплой сразу в прод как осознанный минус в
  `STACK.md`; дешёвая замена наготове — второй compose-профиль на том же
  VM под staging-поддоменом (стек п. 3).
- **TDD на весь прикладной код** — держать под наблюдением: если планка
  начнёт тормозить соло-темп, честнее ослабить правило в `STACK.md`, чем
  молча нарушать (стек п. 5, «мелкое»). Не задача на сейчас — заметка на
  ретро.
- **Изучить способы рекламы и распространения grammashop** — заведено
  20.07.2026 по запросу пользователя. Пока не проработано вообще: какие
  каналы (Telegram-каналы/чаты продавцов, таргет в Telegram Ads, отзывы
  у существующих продавцов площадок-конкурентов, партнёрки/рефералки,
  контент про запуск магазина в Telegram) реально доносят продукт до
  первых продавцов. Требует отдельного обсуждения в диалоге до фиксации
  в `CONCEPT.md` — не решать в одиночку по правилу воркфлоу выше.
- **Студия разработки Auragramma** — заведено 20.07.2026 по запросу
  пользователя: отдельный зонтичный бренд для проектов в экосистеме
  Telegram (grammashop — первый продукт под этим крылом). Нужны:
  отдельный проект/репозиторий, отдельный лендинг, паблик в Telegram.
  Пока не проработано ни позиционирование, ни состав первого лендинга,
  ни то, как Auragramma соотносится с уже написанным про grammashop в
  `CONCEPT.md`/`STACK.md` (отдельный репозиторий или раздел в этом же —
  решить перед стартом). Требует отдельного обсуждения в диалоге, не
  решать в одиночку.
