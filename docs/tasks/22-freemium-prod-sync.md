### Спринт 22: свести прод к фримиум-концепции

Цель: живой продукт (лендинг, платформенная админка, льгота, лимит
карточек) отражает уже принятое 21.07.2026 решение
([CONCEPT.md#тарифы](../CONCEPT.md#тарифы)) — Free/Premium вместо трёх
тарифов 1₽/399₽/999₽. Закрывает расхождение чистовика и прода,
найденное в [ревью 21.07.2026](../reviews/2026-07-21.md). Состав
согласован в диалоге 21.07.2026, следом за ревью.

Решения, зафиксированные при согласовании состава:

- Цена Premium ещё не посчитана (юнит-экономика) — на лендинге вместо
  цифры показывается «По запросу» (ведёт в личку платформы), не
  заглушка-цифра и не скрытие карточки.
- Лимит карточек Premium (3000, рабочий ориентир) получает реальное
  ветвление в коде уже сейчас, хотя самостоятельно (оплатой) на Premium
  пока не попасть — только льготой от админа. Продавцы, которым льготу
  уже выдали в рамках Спринта 21 (tier1), должны фактически получить
  лимит 3000, а не унаследованный Free-лимит 30.

Задачи:

- [ ] Контракт: `subscriptionTiers` (`packages/shared/src/domain/enums.ts`)
  — комментарий с новой семантикой: `tier1` = Free, `tier2` = Premium,
  `tier3` выводится из употребления (не удаляется из enum — миграция БД
  не нужна).
- [ ] Бэк: `grantGrace` (`apps/api/src/services/platform.service.ts`)
  переключить с жёсткого `tier1` на `tier2` — льгота по концепции
  выдаётся только на Premium.
- [ ] Бэк, TDD: лимит карточек по тарифу продавца вместо плоской
  `MAX_PRODUCTS_PER_SELLER = 30` (`products.service.ts`) — Free 30 /
  Premium 3000. Отдельно решить на старте (не является развилкой,
  требующей паузы спринта, но нужно решить до кода): каким лимитом
  считать продавца без записи в `subscriptions` вообще (только что
  зарегистрировался, ещё не получал льготу) — по духу Спринта 21
  («каталог наполняется без оплаты») это должен быть Free-лимит 30, не
  отказ.
- [ ] Фронт: `landing.html` `#tarify` — две карточки Free/Premium вместо
  трёх; цена Premium — «По запросу» со ссылкой в личку платформы (тот же
  контакт, что уже используется в `SellerProfile.tsx`/`lib/platform.ts`);
  заголовок секции и meta-описание («От 1 ₽ в месяц») привести в
  соответствие (два уровня, не «три»).
- [ ] Фронт: `PlatformHome.tsx` — `SUBSCRIPTION_TIER_LABELS` на
  Free/Premium.
- [ ] Фронт, мелкое: подписи с «Тариф 1» в местах, видимых продавцу —
  `SellerProfile.tsx:159` («Реквизиты для перевода»), комментарий в
  `SellerHome.tsx:14`.
