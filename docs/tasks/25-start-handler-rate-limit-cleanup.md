### Спринт 25: `/start`-хендлер бота + rate limiting + уборка бэкапа реструктуризации

Цель: холодный контакт с `@grammashopbot` без диплинка не упирается в тишину;
создание заказов и вебхуки платёжных провайдеров защищены rate limit'ом; репо
не тащит временный бэкап реструктуризации Спринта 23.5. Состав согласован в
диалоге 21.07.2026 (сразу после закрытия Спринта 24) — три независимых от
внешних P0-блокеров (ИП, юрпакет) пункта, специально отделены от крупного
биллинг-движка ЮKassa (следующий спринт, чтобы не размазывать присест).

Задачи:

- [ ] Бэк: минимальный `/start`-хендлер бота. `apps/api/src/bot/client.ts`
  только отправляет уведомления, обработчиков апдейтов нет. Добавить
  update-хендлер к существующему grammY-клиенту: приветствие + кнопка запуска
  Mini App на развилку «О платформе / Открыть магазин» (Спринт 21). Без меню
  команд, один экран.
- [ ] Бэк: rate limiting на API. `@fastify/rate-limit` — лимиты на создание
  заказов и вебхуки ЮKassa/СДЭК, до первого абьюза, а не после.
- [ ] Репо: удалить `docs/_pre-restructure-backup/`. Условие снятия
  («после того как Спринт 24 отработает и станет ясно, что новая структура
  доски не мешает») выполнено — Спринт 24 закрыт, 155/155 тестов, деплой
  прошёл.
