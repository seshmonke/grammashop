### Спринт 25: `/start`-хендлер бота + уборка бэкапа реструктуризации

Цель: холодный контакт с `@grammashopbot` без диплинка не упирается в тишину;
репо не тащит временный бэкап реструктуризации Спринта 23.5. Состав согласован
в диалоге 21.07.2026 (сразу после закрытия Спринта 24), скорректирован в тот же
день анализом перед стартом.

Задачи:

- [ ] Бэк: минимальный `/start`-хендлер бота. `apps/api/src/bot/client.ts`
  только отправляет уведомления, обработчиков апдейтов нет. Добавить
  update-хендлер к существующему grammY-клиенту: приветствие + кнопка запуска
  Mini App на развилку «О платформе / Открыть магазин» (Спринт 21). Без меню
  команд, один экран.
- [ ] Репо: удалить `docs/_pre-restructure-backup/`. Условие снятия
  («после того как Спринт 24 отработает и станет ясно, что новая структура
  доски не мешает») выполнено — Спринт 24 закрыт, 155/155 тестов, деплой
  прошёл.

## Анализ перед стартом

- **Rate limiting (снято)** — `@fastify/rate-limit` уже подключён в
  `app.ts` и уже стоит точечно на `/auth` (`auth.route.ts:19-31`) и на
  создании заказа (`orders.route.ts:24-31`, `config.rateLimit`,
  `ORDERS_RATE_LIMIT_MAX`) — сделано раньше, чем формулировалась эта задача.
  Вебхуков ЮKassa/СДЭК в коде ещё нет вообще (роуты появятся вместе с
  биллинг-движком, следующий спринт) — вешать лимит не на что. Задача
  снята из спринта, лимит на вебхук-роут перенесён в
  [backlog.md](backlog.md) как под-пункт биллинг-движка ЮKassa —
  делается вместе с самим роутом, а не отдельно.
