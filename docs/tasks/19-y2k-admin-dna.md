### Спринт 19: минимальный Y2K-ДНК в админках

Цель: обе админки (продавцовская и платформенная) остаются плоскими и
функциональными, но получают минимальный, согласованный набор бренд-
токенов — единый дизайн-код продукта важнее строгой границы «витрина/
остальное», при условии, что это не трогает плотность/раскладку и не
несёт риска перформанса (blur/glass/scan-lines/glow по-прежнему не
входят). Заведено в диалоге 20.07.2026 (см.
[STACK.md#дизайн-направление](STACK.md#дизайн-направление),
[docs/design/DESIGN_SYSTEM.md#реализация](design/DESIGN_SYSTEM.md#реализация)).
Два пункта, оба одним присестом — правка мелкая, разбивать незачем.

- [x] ~~Unbounded (`font-display`+`y2k-heading`, тот же приём, что на
  витрине) на заголовках экранов — `h1` в `SellerHome.tsx`
  («Товары»), `SellerOrders.tsx` («Заказы»), `PlatformHome.tsx`
  («Продавцы»), `ProductForm.tsx` («Новый товар»/«Изменить товар»)~~ —
  сделано 20.07.2026.
- [x] ~~Магента на главной созидающей кнопке экрана — «Добавить» товар
  (`SellerHome.tsx`) и «Сохранить» карточку (`ProductForm.tsx`,
  submit-кнопка формы). Точечная правка `className` на конкретных
  кнопках, не общий variant в `components/ui/button.tsx` — вторичные
  действия (Изменить/Удалить/Заблокировать/переходы статуса заказа)
  явно используют `outline`/`ghost` уже сейчас, не задеты~~ — сделано
  20.07.2026, `className="bg-magenta text-white hover:bg-magenta/90"`
  на обеих кнопках поверх `cn(buttonVariants(...))` (twMerge резолвит
  конфликт с `bg-primary` из `variant="default"`). Проверено: остальные
  кнопки (`Изменить`/`Удалить`/`Заблокировать`/переходы статуса
  заказа/`Загрузить файл`/`Загрузить фото`/`Добавить вариант`) уже были
  на `outline`/`ghost` — не задеты.
- [x] ~~Сквозная проверка: обе админки на телефоне — заголовки на
  Unbounded, кнопки «Добавить»/«Сохранить» в магенте, раскладка и
  плотность не изменились, вторичные кнопки остались нейтральными~~ —
  подтверждено с телефона 20.07.2026: продавцовская и платформенная
  админки, обе проверены.

**Спринт 19 закрыт** — обе админки получили минимальный Y2K-ДНК
(заголовки на Unbounded, магента на главной созидающей кнопке), плоская
плотная раскладка и нейтральные вторичные действия не тронуты.
