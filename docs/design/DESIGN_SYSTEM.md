# Дизайн-система grammashop

Чистовик. Зафиксировано в диалоге 19.07.2026 (Спринт 10, см.
[tasks/10-design-system.md](../tasks/10-design-system.md)). Направление — **Y2K** (хром, магента, ice,
жирная геометричная типографика), пришло на смену раннему черновику
«тихий минимализм + brass» (см. «История» в конце файла — тот черновик
отклонён целиком, включая символ на весах).

Это спецификация **токенов и правил**, не отчёт о реализации: код
`apps/web` (см. «Не реализовано» ниже) всё ещё на системе из Спринта 9
(brass/Fraunces). Перевод интерфейса на эту систему — отдельная будущая
задача.

## Принцип: один слой, не замена

Витрина живёт внутри чужого приложения — фон, текст, light/dark
приходят из Telegram покупателя (`themeParams`, см.
[STACK.md#авторизация](../STACK.md#авторизация)). Это уже проверенное
преимущество: магазин ощущается частью Telegram, а не отдельным вебвью.
Дизайн-система не спорит с этим фактом, а работает **поверх** него:
акцентный цвет, вордмарк, символ, ритм типографики — то немногое, что
принадлежит нам, а не клиенту покупателя.

**Архитектура акцента и тарифная сетка** (см.
[CONCEPT.md#тарифы](../CONCEPT.md#тарифы)): с Тарифа 2 продавец получает
свой цвет и толщину линий, с Тарифа 3 — свой фон. Значит:

- **Витрина продавца** — токены ниже это дефолт Тарифа 1 и стартовое
  состояние Тарифа 2/3 до кастомизации продавцом. Не жёстко зашитый цвет
  платформы.
- **Продавцовская и платформенная админки, аватарка бота, будущий
  лендинг** — токены неизменны. Это айдентика grammashop, не продавца, и
  кастомизации продавцом не подлежит.

## Палитра

| Токен | Hex | Роль |
|---|---|---|
| `--void` | `#0A0A0C` | глянцевый чёрный, база тёмных поверхностей бренда |
| `--void-2` | `#131317` | карточки/панели поверх `--void` |
| `--void-3` | `#1B1B21` | ещё один уровень поверхности (глубже вложенные блоки) |
| `--chrome` | `#B8C4D0` | материал: заголовки, обводки, структура |
| `--chrome-light` | `#F0F4F8` | верхний блик хром-градиента |
| `--chrome-dark` | `#5A6572` | нижняя тень хром-градиента, приглушённый текст на брендовых поверхностях |
| `--magenta` | `#FF2E9A` | акцент — **коммерция/ценность**: CTA, цены, скидки |
| `--magenta-deep` | `#C4157A` | magenta, калибровка под светлые поверхности (не «слепнет» на паппире/белом) |
| `--ice` | `#6EE7FF` | акцент — **интерактив/статус/навигация**: ссылки, фокус с клавиатуры, номера, статус-точки, состояния ожидания |
| `--text` | `#F0F0F2` | основной текст на тёмных брендовых поверхностях |
| `--text-dim` | `#8A8F98` | приглушённый текст |
| `--rule` | `rgba(184, 196, 208, 0.22)` | тонкие разделители |

**Смысловое разделение двух акцентов — не декоративное дублирование:**
magenta несёт коммерческую информацию (что стоит денег/приносит
действие), ice — интерактивную/статусную (что кликабельно, в фокусе,
ожидает ответа). Правило при добавлении новых элементов: сначала
определить, к какой из двух категорий относится элемент, потом красить.

**Где именно magenta, а где нет** (уточнено 22.07.2026, Спринт 28 — см.
находки в [tasks/28-storefront-design-review.md](../tasks/28-storefront-design-review.md)):
правило выше про категории было декларативным, без явного критерия
«какая именно кнопка коммерческая». Конкретно:
- Любая цена, показываемая покупателю как сумма (цена товара, «Итого» в
  корзине/чекауте, сумма заказа на экране успеха) — всегда
  `y2k-price-glow` + `text-magenta-on-theme`, без исключений.
- CTA, непосредственно завершающая или продвигающая покупку (добавить в
  корзину, оформить заказ, написать продавцу для оплаты) — сплошная
  заливка `bg-magenta text-white` + `y2k-cta-glow` (не
  `text-magenta-on-theme` — кнопка на собственной заливке, контраст
  самодостаточен, см. «Реализация»). Служебные/навигационные элементы
  (степпер количества, «удалить», «назад») — нейтральные, без magenta.

**Статус «нет в наличии» — не destructive**: красный (`text-tg-
destructive`) зарезервирован за ошибками действия пользователя
(невалидная форма, неудачный запрос) — наличие товара не ошибка, а факт
каталога, поэтому везде — нейтральный бейдж (`text-tg-hint` на
контрастной непосредственному родителю поверхности), не красный текст.

**Деструктивные действия (удалить/отменить/заблокировать) — не
красятся в destructive** (уточнено 22.07.2026, Спринт 29 — см. находку
5 в [tasks/29-admin-design-review.md](../tasks/29-admin-design-review.md)):
`text-tg-destructive` — только для текста ошибки (см. правило выше), не
для самих кнопок действия. Кнопка необратимого/деструктивного действия
— нейтральная `Button variant="ghost"`, тот же визуальный вес, что и у
любого другого второстепенного действия экрана; выделять её красным не
нужно — контекст (текст кнопки, соседний `confirm()`) уже достаточно
явно называет последствие. Любое действие, необратимое и бьющее в
API немедленно (не черновик формы до сохранения) — всегда с `confirm()`
перед вызовом, без исключений.

**Успех — отдельный токен `--tg-success`** (уточнено 22.07.2026, Спринт
29, находка 1 в [tasks/29-admin-design-review.md](../tasks/29-admin-design-review.md)):
находка изначально выглядела как «`SellerProfile.tsx` красит
подтверждение сохранения в ничем не обоснованный `emerald-500`», но при
проверке (`grep` по всему `apps/web/src`, не по памяти) тот же голый
`emerald-500` уже был и в canon-реализации формы (`CheckoutPage.tsx`,
зелёная обводка валидного тронутого поля) — то есть «успех» уже
де-факто существовал как визуальный язык проекта, просто не был
токенизирован и потому разъехался (текст в одном месте, обводка в
другом, оба — сырой Tailwind-класс). Решение — не убирать зелёный, а
закрепить его: `--tg-success` (`#10b981`, то же значение, что уже было
в обоих местах) добавлен в палитру наравне с `--tg-destructive`
(Telegram themeParams не даёт токена под «успех», значение
фиксированное, не тема-зависимое). Применим и к тексту подтверждения
операции, и к обводке валидного поля — семантически один и тот же
сигнал «действие завершилось успехом», просто в двух разных виджетах.
`SellerProfile.tsx` переведён на `text-tg-success` в этом спринте;
`CheckoutPage.tsx` вне скоупа Спринта 29 (только админки) — перевод его
`border-emerald-500` на токен зафиксирован в
[backlog.md](../tasks/backlog.md) отдельным пунктом, не сделан попутно.

Rationale выбора: chrome (не золото/brass) — материал-сигнатура
Y2K-эпохи (1999-2003), magenta+ice — классическая Y2K-пара
(cyber-bubblegum + ice-cyber), проверено по актуальным источникам
19.07.2026 (не по памяти) — см. «Источники» ниже.

## Типографика

**[Unbounded](https://fonts.google.com/specimen/Unbounded)** (Google
Fonts) — вариативный шрифт, ось `wght` 200–900 в одном файле,
**с кириллицей** (в отличие от раннего черновика на Fraunces, у которой
кириллицы нет вообще — см. «История»).

```css
@font-face {
  font-family: 'Unbounded';
  src: url('.../unbounded-cyrillic.woff2') format('woff2');
  font-weight: 200 900;
}
@font-face {
  font-family: 'Unbounded';
  src: url('.../unbounded-latin.woff2') format('woff2');
  font-weight: 200 900;
  unicode-range: U+0000-00FF, U+2000-206F;
}
```

Google Fonts CSS2 API отдаёт **один и тот же файл** на разные
`font-weight` в декларации (variable font) — вес регулируется явным
`font-variation-settings: 'wght' N`, а не выбором другого файла.
Использование:

```css
.display-heavy { font-variation-settings: 'wght' 900; } /* хиро, вордмарк */
.display-mid   { font-variation-settings: 'wght' 700; } /* заголовки секций */
.display-label { font-variation-settings: 'wght' 500; } /* лейблы, eyebrow */
```

**Только для дисплейных моментов** — вордмарк, заголовки секций,
крупные числа/цены. Тело интерфейса остаётся на системном стеке
(`-apple-system, BlinkMacSystemFont, "Segoe UI", Roboto, Helvetica,
Arial, sans-serif`) — веб-шрифт в рантайме основного текста не грузим
намеренно, это ограничение перфоманса в Telegram WebView на бюджетных
Android (см. [STACK.md#вёрстка](../STACK.md)), не вопрос тренда, и оно
не пересматривалось Y2K-поворотом.

## Приёмы

- **Хром-градиент на тексте** — только на вордмарке (`gramma` в
  `linear-gradient(180deg, --chrome-light, --chrome 45%, --chrome-dark)`
  через `background-clip: text`), не на заголовках секций — решено
  явно 19.07.2026, не расширять без нового решения.
- **Glow** — magenta-glow на CTA/цены/скидки (`text-shadow`/`box-shadow`
  с `--magenta` низкой непрозрачности), ice-glow на интерактивных/
  статусных элементах. Символ пары: сигнатурный приём эпохи, не
  использовать на инертных/неважных элементах — обесценится.
- **Скан-линии** — едва заметная фактура фона, `repeating-linear-gradient`
  горизонтальными линиями каждые 3px, непрозрачность 3.5%:
  ```css
  background: repeating-linear-gradient(
    0deg,
    rgba(184, 196, 208, 0.035) 0px,
    rgba(184, 196, 208, 0.035) 1px,
    transparent 1px,
    transparent 3px
  );
  ```
  Решено 19.07.2026 — интенсивность **не усиливать**.

## Формы

Зафиксировано в диалоге 22.07.2026 (вне спринта — концептуальное решение,
см. [tasks/milestones.md](../tasks/milestones.md)). Правила разметки и
поведения форм — не только визуал, но и валидация: раскол между экранами
(`ProductForm`/`RegisterForm`/`SellerProfile` дизейблили submit до
валидности формы, `CheckoutPage` валидировал вживую с инлайн-ошибками)
показал, что без явного правила поведение расползается так же, как и
вёрстка. Заземлено на исследовании 22.07.2026 (Baymard Institute, NN/g,
LukeW), не на памяти — см. «Источники».

**Раскладка полей**:
- Один столбец, одно поле в строке — не многоколоночные формы. Baymard:
  multi-column даёт больше пропущенных и перепутанных полей;
  CXL-измерения — single-column проходят в среднем на 15 секунд быстрее.
- Исключение — только жёстко спаренные поля с очевидной смысловой связью
  (город+индекс, дата+CVC). В текущих формах grammashop таких пар нет —
  правило декларативное, на будущее, не применяется нигде сейчас.
- Ширина поля — всегда на всю ширину контейнера формы (`w-full`), без
  вариаций по типу поля.
- Лейбл — сверху над полем (top-aligned), не сбоку: на мобильном экране
  (а именно так открывается интерфейс в Telegram Mini App) это не требует
  масштабирования и читается за одну фиксацию взгляда.

**Обязательные/необязательные поля**: в формах grammashop бо́льшая часть
полей обязательна — по актуальной рекомендации NN/g в этом случае
помечать нужно не обязательные поля звёздочкой, а меньшинство —
**необязательные**, суффиксом `(необязательно)` в тексте лейбла.
Обязательные поля — без пометки вообще.

**Валидация и ошибки** — канон: `CheckoutPage.tsx` +
`checkout/validate-checkout-form.ts`.
- Проверка поля — по `blur` (после того как пользователь ушёл с поля), не
  на каждое нажатие клавиши: ошибка, всплывающая посреди набора текста,
  выглядит как обвинение до того, как ввод закончен, и на мобильном
  сдвигает разметку под пальцем.
- Ошибка — текстом под конкретным полем (не одной общей строкой под всей
  формой), плюс подсветка рамки поля и `aria-invalid`/`aria-describedby`
  на самом инпуте.
- Кнопка submit **не дизейблится по невалидности формы** — только на
  время самого запроса (`isPending`). Дизейбл до полной валидности —
  антипаттерн: пользователь не понимает, почему кнопка не нажимается.
  По попытке сабмита с ошибками — все поля помечаются «тронутыми», ошибки
  подсвечиваются, отправка блокируется валидацией, а не состоянием
  кнопки.

Переведены на канон Спринтом 29 (22.07.2026, см.
[tasks/29-admin-design-review.md](../tasks/29-admin-design-review.md)):
`RegisterForm.tsx`, `SellerProfile.tsx`, `ProductForm.tsx`.

## Навигация назад

Зафиксировано в диалоге 22.07.2026 (Спринт 29, см. находку 2 в
[tasks/29-admin-design-review.md](../tasks/29-admin-design-review.md)) —
до этого решения на шести экранах админок было три разных ад-хок
реализации возврата на родительский экран (кнопка в шапке, голая
текстовая ссылка со стрелкой над заголовком, полное отсутствие
навигации в шапке).

Когда у экрана есть естественный «родительский» экран (не корень
раздела) — ссылка на него всегда `Button asChild variant="outline"
size="sm"` в шапке, в одном ряду с заголовком (тот же слот, что уже
используют кросс-навигационные кнопки вроде «Платформа»/«Заказы»/
«Настройки»). Не отдельная текстовая ссылка со стрелкой, не кнопка вне
шапки (низ формы/экрана) — это единственный слот для «уйти с экрана».

## Навигация — floating toolbar

Зафиксировано в диалоге 23.07.2026 (`/grill-me`), уточнено в диалоге тем
же днём после реализации (правильное название паттерна, форма, «капля»,
переиспользование в админках). Отправная точка — у витрины не было ни
одного постоянного nav-элемента: переход между разделами только через
клики (карточка → деталь, кнопка корзины → `/cart` → `/checkout`),
разделов верхнего уровня для покупателя всего два (Каталог, Корзина).

**Источник паттерна и правильное название — Apple HIG, floating toolbar**
(не «bottom tab bar» — так паттерн назывался в черновике до уточнения
23.07.2026; официальный Apple Design Resources kit, iOS 27, проверено
23.07.2026 — developer.apple.com/design/resources/#ios-apps подтверждает
версию). Берётся только навигационный паттерн (структура и поведение),
не визуальный язык — цвет/материал/типографика остаются Y2K (у витрины;
у админок — нейтральный акцент, см. ниже), никакого стеклянного (glass)
материала Apple. Из официального kit'а токены/ассеты сознательно не
выгружались — структурные правила (иконка+подпись, минимальный
тап-таргет, safe-area-bottom, badge на иконке) не зависят от конкретной
версии iOS, а визуал целиком берётся из палитры выше.

**Форма — капсула (`rounded-full`), а не скруглённый прямоугольник**:
торцы максимально круглые (радиус = половина высоты бара), не
фиксированный `rounded-2xl`/`rounded-3xl` с более пологим скруглением
углов. Плавающая, с отступами от краёв экрана — не докнутый full-width
footer. Это осознанное исключение из принципа «берём только паттерн, не
визуал»: сама форма капсулы — часть визуальной сигнатуры Apple Liquid
Glass (iOS 26/27), не нейтральная форма — решение принято явно в
диалоге, а не как побочный эффект.

**Максимальная ширина — 430px, центрирована** (решено 23.07.2026,
`/grill-me`, задача на реализацию —
[backlog.md](../tasks/backlog.md#кап-ширины-floating-toolbar)). До этого
решения капсула держалась только на `mx-4` (16px от краёв экрана) без
верхней границы — на широком десктопном Telegram-клиенте (то же явление,
что решалось для сетки каталога в Спринте 38: окно резайзится
произвольно, паттерн проектировался под телефон) капсула растягивалась
почти во весь экран, что заметно на скриншотах Спринта 38 при 1920px.
Material Design (m3.material.io/foundations/layout/breakpoints) и Apple
HIG сходятся, что bottom/floating tab bar — паттерн compact-ширины
(телефон); «правильный» ответ по букве гайдлайнов — боковой navigation
rail на широких экранах. Отклонено явно: у витрины и админок всего 2-4
пункта меню, Desktop-клиент Telegram — вторичная площадка, замена паттерна
навигации — несоразмерно тяжёлое решение ради второстепенного сценария.
Вместо этого — тот же приём, что `--catalog-max-width`: `max-width` на
уровне самой капсулы (не внешнего sticky-контейнера), значение — ширина
самого широкого распространённого телефона (~428-430px, iPhone Pro Max),
задокументированный токен в `index.css`, не голое число в разметке.
Применяется на общем примитиве `FloatingToolbar.tsx` — чинит сразу
`TabBar.tsx` (витрина) и `AdminToolbar.tsx` (админки) одним изменением.
Плашка сводки чекаута над капсулой в `CartPage` (`above`-слот
`FloatingToolbar`) — другой визуальный язык (полноширинный glass-бар с
верхней границей, не капсула, тот же паттерн, что sticky checkout-бар у
крупных площадок) — остаётся full-bleed, кап не затрагивает.

Отдельная боковая панель категорий товара — не то же самое, что
navigation rail выше (это контентная навигация внутри раздела «Каталог»,
не замена Каталог/Корзина/Заказы) — остаётся нерешённым пунктом бэклога
([категории товаров](../tasks/backlog.md#категории-товаров-в-каталоге-продавца)),
этим решением не закрыта.

**«Капля» — подсветка активного раздела, перетекающая между вкладками**
(уточнено 23.07.2026, как на iOS floating toolbar/tab bar: заливка-
капсула позади активной иконки, при переключении не исчезает и
появляется заново, а *едет* к новой позиции). Реализация —
`position: absolute` капсула на `translateX(activeIndex × 100%)` с
transition на `transform` (`FloatingToolbar.tsx`), без сторонней
библиотеки анимации (`framer-motion` и т.п. в зависимостях `apps/web`
нет, не заводили ради одного эффекта). Капля — не сплошная заливка
акцентным цветом (тот тон уже занят текстом/иконкой активной вкладки,
поверх которого капля лежит) — полупрозрачная тонировка того же токена
(`/15` alpha), у витрины `--ice-on-theme`, у админок `--tg-accent` (см.
ниже). Меньше двух вкладок — капле некуда «течь», тулбар в этом случае
не рендерится вовсе.

**Материал капсулы — `.tg-glass-toolbar`, заметно прозрачнее `.tg-glass`
шапок разделов** (запрошено в диалоге, Спринт 36: под капсулой всегда
контент экрана — фото каталога, не текст, которому нужен твёрдый фон для
читаемости; отдельный класс, `.tg-glass` шапок не тронут). Прозрачный фон
поверх произвольного, непредсказуемого контента (в отличие от плоской
`--tg-bg`/`--tg-surface` под шапками) потребовал четырёх независимых
правок контраста:

- **Блюр не рендерился на мобильном (iOS)** — известный баг WebKit/
  Firefox: `backdrop-filter` не применяется на элементе с `border-radius`
  внутри `position: sticky`-родителя без форсированного GPU-слоя ([Mozilla
  bug 1803813](https://bugzilla.mozilla.org/show_bug.cgi?id=1803813));
  капсула (`rounded-full`, потомок sticky-подвала) — ровно этот случай,
  шапок (без `border-radius`) баг не касается. Фикс — `transform:
  translateZ(0)` на `.tg-glass-toolbar`.
- **Текст неактивных вкладок** красился в `--tg-hint` (`#8a8a8e`,
  одинаково серый в обеих темах Telegram) — сливался с посветлевшим от
  блюра фоном на светлой теме (на тёмной тот же серый читался нормально,
  отсюда разница жалоб по темам). Заменён на `--tg-text` — токен уже
  темизирован самим Telegram-клиентом (тёмный на светлой теме, светлый на
  тёмной), хардкод по теме не нужен.
- **Активный текст/иконка и обводка капли** — по тем же причинам не
  читались в исходных `--ice-on-theme`/`--tg-accent`. Красить активную
  вкладку в `--tg-text`, как неактивные, отклонено в диалоге (`/grill-me`)
  — стёрло бы связь «капля тонирована тем же токеном, что активный текст»
  (см. выше). Вместо этого заведены темнее смешанные
  `--ice-on-theme-toolbar`/`--tg-accent-on-theme-toolbar` (тот же приём
  on-theme, но смесь с `--tg-text` весомее — 45/55 и 55/45 вместо 65/35) —
  только для активного текста и новой обводки капли; заливка капли (`/15`
  alpha) осталась на исходных `--ice-on-theme`/`--tg-accent`, чтобы не
  превратиться в сплошное пятно. `--tg-accent` такого on-theme варианта
  раньше не имел вовсе (был только у `--ice-on-theme`, для фокус-кольца) —
  заведён впервые здесь.
- Иконка+подпись любой вкладки (активной и неактивной) дополнительно несут
  `filter: drop-shadow` (`.toolbar-label-shadow`) — не `text-shadow`,
  действует и на SVG-иконки Lucide, не только на текст; не альтернатива
  токенам выше, а дополнительный контраст поверх непредсказуемого фото.

**Поведение — статичное иначе**, без scroll-driven сжатия/скрытия
самого бара (в отличие от референса Liquid Glass, где весь бар морфится
при скролле — эта часть паттерна НЕ взята). Причина: витрина/админки —
Telegram Mini App внутри WebView, которая уже сама шлёт свои
resize-события (клавиатура, `expand`/`collapse`); ещё один independent
scroll-listener, меняющий геометрию бара, — источник гонки между двумя
системами ресайза. Капсула визуально висит на месте, единственная
анимация — капля внутри неё; позиционирование — `position: sticky` в
подвале flex-колонки страницы, не `fixed` (Спринт 36: на Telegram
Desktop/macOS `fixed`-тулбар «ехал» при переключении вкладок — клиент
неверно пересчитывает вьюпорт при рефлоу контента, ни один предок в
дереве и сам SDK-скрипт тут ни при чём; `sticky` считается в обычном
потоке/относительно ближайшего скролл-контейнера, не задевает
проблемную область измерения клиента).

**Видимость — скрывается только на flow-экранах с формой/шагом
процесса**: `CheckoutPage` (`/checkout`) и формы редактирования админок
не показывают бар вовсе (страница просто не подключает компонент, без
логики по `pathname`) — там у экрана уже есть свой явный «шаг вперёд»
(сабмит формы), а не место для смены раздела.

`ProductDetail` (`/product/:id`) — исключение из первоначального
решения (уточнено в диалоге 23.07.2026, тем же днём): карточка товара
всё-таки **показывает** бар, а не прячет — иначе переход «посмотрел
товар → хочу в корзину» требовал двух шагов (сначала «назад» на
`/`, потом вкладка «Корзина») вместо одного. Кнопка «← Назад» в шапке
карточки убрана целиком — «Каталог» в баре её полностью заменяет
(`/`), «Корзина» ведёт в `/cart`. Требование — бар не должен
перекрывать цены/варианты товара внизу карточки: со sticky-подвалом
(Спринт 36) это уже не magic-number нижний отступ под `main` — страница
`flex flex-col`, `main` — `flex-1`, тулбар просто следующий элемент в
потоке, содержимое никогда не оказывается под ним.

**Переиспользуется в двух контекстах** — общий примитив
`FloatingToolbar.tsx` (капсула + капля + раскладка), раскраска
параметризована:

- **Витрина** (`TabBar.tsx`) — Каталог (`/`), Корзина (`/cart`, badge
  количеством товаров). Акцент — ice-семья (навигация/интерактив по
  правилу палитры «ice — акцент интерактив/статус/**навигация**»), не
  magenta (тот зарезервирован за коммерческими CTA/ценами, см. «Где
  именно magenta»): заливка капли — `--ice-on-theme`, активный текст и
  обводка капли — темнее смешанный `--ice-on-theme-toolbar` (см. выше).
  `MiniCartBar` убран полностью — функционально дублировал бы вкладку
  «Корзина» (оба вели в `/cart`, оба внизу экрана).
- **Продавцовская/платформенная админка** (`AdminToolbar.tsx`) —
  Товары (`/seller`), Заказы (`/seller/orders`), Настройки
  (`/seller/profile`); у пользователей с `isAdmin` — плюс «Платформа»
  (`/platform`). Акцент — `--tg-accent` (нативный акцент Telegram-темы),
  не ice/magenta — админки не получают брендовый Y2K-акцент (см.
  `STACK.md#дизайн-направление`): заливка капли — `--tg-accent`, активный
  текст и обводка капли — темнее смешанный `--tg-accent-on-theme-toolbar`
  (см. выше). Разделы продавца и
  «Платформа» — независимые условия показа (`sellerId`/`isAdmin` —
  разные признаки сессии, см. `auth.service.ts`), у чистого платформенного
  админа без своего магазина был бы один пункт — тулбар в этом случае не
  рендерится (см. правило «меньше двух вкладок» выше). Заменяет прежние
  дублирующие кнопки-ссылки в шапках `SellerHome`/`SellerOrders`/
  `SellerProfile`/`PlatformHome`.

**Вне скоупа**: третья вкладка витрины «Заказы» (покупателя) — у
покупателя нет бэкенд-эндпоинта для получения списка своих заказов
(есть только `POST /shop/:sellerId/orders` и продавцовские
`GET/PATCH /seller/orders`), это отдельная фича — вынесено отдельным
пунктом в [backlog.md](../tasks/backlog.md). «Профиль» покупателя как
раздел не заводится вовсе — авторизация через Telegram делает отдельный
экран профиля избыточным.

Реализовано в [Спринте 33](../tasks/33-bottom-tab-bar-navigation.md) —
`apps/web/src/nav/FloatingToolbar.tsx`, `TabBar.tsx`, `AdminToolbar.tsx`
(файл спринта сохраняет старое рабочее название в имени — история не
переименовывается, см. `tasks/INDEX.md`).

## Состояния (загрузка/пусто/ошибка)

Зафиксировано в диалоге 22.07.2026 (Спринт 28, см.
[tasks/28-storefront-design-review.md](../tasks/28-storefront-design-review.md)).
До этого решения на пяти экранах витрины было три разные ад-хок
реализации одного и того же паттерна — заземлено на исследовании NN/g
(см. «Источники»), не на памяти.

**Два варианта, один компонент** (`ScreenState`,
`apps/web/src/shop/ScreenState.tsx`):
- `variant="full"` — заменяет экран целиком, без шапки. Только когда
  шапке ещё нечего показать: личность магазина/товара ещё не
  разрешена (переход по протухшей/неверной ссылке, сам каталог ещё
  грузится).
- `variant="inline"` — живёт внутри уже отрисованной шапки, в `<main>`.
  Во всех остальных случаях: контекст (магазин/товар/корзина) уже
  известен, прятать шапку и терять навигацию («Назад», заголовок
  экрана) незачем.

**Компонент общий для всего проекта, не только витрины** (уточнено
22.07.2026, Спринт 29, находка 3): админки (`SellerHome`,
`SellerOrders`, `SellerProfile`, `PlatformHome`) на момент находки
дублировали тот же паттерн ад-хок инлайн-параграфами вместо
переиспользования `ScreenState` — всегда `variant="inline"` в этих
экранах (шапка везде уже отрисована на момент состояния). Правило
компонента не ограничено покупательским путём.

**Любое пустое/ошибочное состояние — с путём дальше**, если он
объективно существует (NN/g: объяснить, что произошло, и что делать
дальше). Исключение — состояния, где действия у покупателя правда нет
(пустой каталог продавца: покупателю нечего делать, кроме как уйти).

**Заголовки состояний — не дисплейный шрифт**: это служебные,
одноразовые сообщения («Загрузка…», «Корзина пуста»), не заголовки
секций — `font-display` остаётся только за постоянными элементами
экрана (правило «Только для дисплейных моментов» в разделе
«Типографика» не пересматривается).

## Символ и логотип

Файл: [`logo.svg`](logo.svg) — хромированная гиря (калибровочный груз:
купол-«гриб» + перехват-«талия» + цилиндрическое тело), сгенерирована
через AI-инструмент с нативным векторным рендерингом (не хендкод SVG —
см. «История»). Многотональный рендер (белый/серый/чёрный) уже
соответствует токену `--chrome` — **не перекрашивать**.

Правила использования:
- Фон в файле отсутствует (удалён при интеграции) — знак самостоятельный,
  ложится на любую поверхность. Проверено на `--void`, на `--chrome`, на
  белом.
- На светлом `--chrome`-фоне контраст слабее (белые блики частично
  сливаются с фоном) — не проблема на практике: `--chrome` в системе не
  используется как заливка под крупными элементами, только текст/обводки.
- Держится вплоть до 16px (проверено вживую рендером, не на глаз) —
  безопасно использовать как favicon.
- Не растягивать не пропорционально, не добавлять обводку/тень поверх
  уже отрендеренных бликов — испортит читаемость металла.

Лок-ап с вордмарком: знак слева, `gramma┃shop` справа, разделитель между
`gramma` и `shop` — вертикальная magenta-линия с glow (та же роль, что у
хром-градиента в вордмарке — сигнатурная деталь, не декоративная).

## Готовый CSS (токены)

```css
:root {
  --void: #0a0a0c;
  --void-2: #131317;
  --void-3: #1b1b21;
  --chrome: #b8c4d0;
  --chrome-light: #f0f4f8;
  --chrome-dark: #5a6572;
  --magenta: #ff2e9a;
  --magenta-deep: #c4157a;
  --ice: #6ee7ff;
  --text: #f0f0f2;
  --text-dim: #8a8f98;
  --rule: rgba(184, 196, 208, 0.22);
  --display: 'Unbounded', 'Arial Black', sans-serif;
  --sans: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Helvetica, Arial, sans-serif;
}
```

## Реализация

Токены и приёмы применены в коде `apps/web` Спринтом 17 (20.07.2026,
[tasks/17-y2k-storefront-design.md](../tasks/17-y2k-storefront-design.md)), расширены Спринтом 28
(22.07.2026, [tasks/28-storefront-design-review.md](../tasks/28-storefront-design-review.md)) на весь
покупательский путь: `index.css` (токены + утилити-классы),
`StorefrontHome.tsx` (флюидная сетка каталога), `ProductCard.tsx`,
`ProductDetail.tsx`, `CartPage.tsx`, `CheckoutPage.tsx`. Полный акцент
(сетка/фото/тема продавца) на админки и лендинг не распространяется по
принципу «не везде, где витрина продавцу не принадлежит» (см. выше) — но
это не значит, что у них одинаковый объём Y2K: у сетки/фото/темы
продавца просто нет предмета применения на статической странице и в
служебном интерфейсе (нет каталога, нет продавца), это не то же самое,
что осознанное исключение.

**Уточнено 22.07.2026 (Спринт 30):** предыдущая формулировка ошибочно
числила glow и скан-линии в этом же «не распространяется» списке заодно
с сеткой/фото/темой продавца. Это верно для админок (`Спринт 19` —
минимальный ДНК ниже), но не для лендинга: `landing.css` несёт полную
палитру, glow (`.cta`, `.word-carousel`, `.premium-cta`) и скан-линии
фона — с первого коммита (Спринт 15, 19.07.2026, тот же день, что и
поворот на Y2K), не как позднейшее расширение. Лендинг — не «Y2K не
касается» и не «минимальный ДНК», а полноценная реализация системы
наравне с покупательским путём; из «полного акцента» ему действительно
не достаётся только то, чему на статической странице нет предмета
(сетка каталога, фото товара, кастомизация под тему продавца).

**`Fork.tsx` (Спринт 30, 22.07.2026)** — экран-развилка для пользователя
без роли (`auth/Fork.tsx`) относится к той же категории «платформенная,
не продавцовская поверхность», что и лендинг и покупательский путь, не
к категории админок — аудит нашёл, что при этом он не нёс ни
`y2k-scanlines`, ни `y2k-cta-glow` на основной кнопке, ни лок-ап
вордмарка (см. «Символ и логотип» выше). Все три добавлены: скан-линии
на корневой контейнер, `y2k-cta-glow` на кнопку «Запустить магазин»
(тот же класс, что и у всех коммерческих CTA покупательского пути —
`CartPage`, `CheckoutPage`, `ProductDetail`, `ProductCard`), лок-ап
(`logo.svg` + `gramma┃shop`) вместо голого текста — Fork является
единственным экраном в `apps/web` (кроме лендинга), где на экране
показывается название платформы, а не заголовок раздела, так что лок-ап
здесь уместен так же, как в шапке лендинга.

**Ревью админок (Спринт 29, 22.07.2026, см.
[tasks/29-admin-design-review.md](../tasks/29-admin-design-review.md))**
распространил на `SellerHome.tsx`, `ProductForm.tsx`, `RegisterForm.tsx`,
`SellerOrders.tsx`, `SellerProfile.tsx`, `PlatformHome.tsx` то, что
границей выше не запрещено (это не Y2K-акцент, а общепроектные
паттерны): единый компонент `ScreenState` вместо ад-хок инлайн-состояний,
единый паттерн навигации назад (`Навигация назад` выше), канон форм
(`Формы` выше) для всех трёх форм, нейтральная (не destructive) окраска
кнопок необратимых действий, токен `--tg-success`.

**Находка при переводе в код**: чистый `--magenta`/`--ice` текстом
поверх переменной `--tg-surface`/`--tg-bg` (светлая тема Telegram)
проваливает контраст — эти цвета калибровались на прототипах против
`--void` (тёмная брендовая поверхность), не против произвольной темы
клиента. Решение — токен `--magenta-on-theme` (`color-mix` с
`--tg-text`, детали в `tasks/17-y2k-storefront-design.md`), `--ice` текстом на
`--tg-surface` не используется вовсе (только `--tg-link`/`--tg-text`
в этих местах). Не пересматривает саму палитру — только то, где именно
подставляется голый `--magenta`/`--ice`, а где адаптированный вариант.

**Тот же приём для ice — фокус с клавиатуры (Спринт 28)**: аудит нашёл,
что `:focus-visible` в коде красился в `--tg-accent`, хотя по палитре
фокус — ice-категория; сырой `--ice` обводкой поверх произвольной темы
Telegram рискует тем же провалом контраста, что и magenta текстом (см.
выше) — на светлой теме бледная обводка едва видна. Решение то же:
`--ice-on-theme` (`color-mix` с `--tg-text`), используется только для
обводки `:focus-visible`, не для текста/фона — сама палитра ice
остаётся прежней.

**Минимальный ДНК в админках — уточнено 20.07.2026 (Спринт 19)**:
принцип «обе админки не подключают Y2K» выше — не про нулевой контакт с
брендом, а про то, что админки не получают полный акцент (сетку/фото/
glow/скан-линии/тему продавца). Ради единого дизайн-кода продукта
(запрошено в диалоге — цельность инструментов важнее строгой границы
«витрина/остальное») туда переезжают две вещи: `font-display`
(Unbounded) на заголовках экранов и `--magenta`/`--magenta-on-theme`
как акцент главной созидающей кнопки экрана («Добавить»/«Сохранить»).
Всё остальное (плотная плоская раскладка, нейтральные вторичные
действия, отсутствие glass/scan-lines) — без изменений, см.
[STACK.md#дизайн-направление](../STACK.md#дизайн-направление).

Также не сделано: аватарка `@grammashopbot` в BotFather (ручная загрузка
вне кода) — растровый favicon (`.ico`/PNG-набор из `logo.svg`) сделан
Спринтом 31 на обеих публичных поверхностях (`apps/web/index.html`,
`apps/web/public/landing.html`).

## Источники

Направление и палитра заземлены на исследовании 19.07.2026, не на
памяти модели:
- [Webflow — Y2K aesthetic guide](https://webflow.com/blog/y2k-aesthetic)
- [Y2K Color Palette — hex codes](https://graphicloads.com/y2k-color-palette-hex-codes/)
- [Figma — 2026 web design trends](https://www.figma.com/resource-library/web-design-trends/)

Правила форм (раздел «Формы») заземлены на исследовании 22.07.2026:
- [Baymard Institute — Avoid Extensive Multicolumn Layouts](https://baymard.com/blog/avoid-multi-column-forms)
- [NN/g — Marking Required Fields in Forms](https://www.nngroup.com/articles/required-fields/)
- [LukeW — Top, Right or Left Aligned Form Labels](https://www.lukew.com/ff/entry.asp?504=)

Правила состояний (раздел «Состояния») заземлены на исследовании
22.07.2026:
- [NN/g — Designing Empty States in Complex Applications](https://www.nngroup.com/articles/empty-state-interface-design/)
- [NN/g — Error-Message Guidelines](https://www.nngroup.com/articles/error-message-guidelines/)
- [NN/g — Skeleton Screens vs. Progress Bars vs. Spinners](https://www.nngroup.com/videos/skeleton-screens-vs-progress-bars-vs-spinners/)

Паттерн навигации (раздел «Навигация — bottom tab bar») заземлён на
источнике пользователя, проверено 23.07.2026:
- [Apple Design Resources — iOS & iPadOS apps](https://developer.apple.com/design/resources/#ios-apps)
  (официальный kit, версия iOS 27 подтверждена напрямую с этой страницы —
  не по памяти модели, чей срез знаний не доходит до этой версии)

## История

- **Черновик 1-2 (18-19.07.2026, отклонён целиком)** — тихий минимализм
  по референсам азиатской fashion-индустрии (Markaware, 29CM, W
  Concept, Ader Error), палитра Ink/Paper/Brass, вордмарк на Fraunces
  (серифная антиква без кириллицы). Символ перебирал через несколько
  гипотез — весы (отклонено: не относится к смыслу проекта), монета с
  самолётиком Telegram (отклонено), змея вокруг пачки денег (отклонено:
  ручной SVG не держал форму), гиря/калибровочный груз хендкодом
  (отклонено: «получается шляпа» — суть формы была верной, но ручное
  написание SVG-координат — плохой инструмент для векторной
  иконографии).
- **Поворот на Y2K (19.07.2026)** — по прямому решению: «жирные прямые
  шрифты», хром вместо золота. Символ решён генерацией через AI-
  инструмент (не хендкод) — та же форма гири, что и в отклонённом
  черновике, но исполнение через нормальный инструмент вместо ручных
  bezier-координат.
