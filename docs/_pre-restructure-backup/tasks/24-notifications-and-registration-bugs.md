### Спринт 24: уведомления продавцу + баги регистрации

Цель: продавец в проде реально получает уведомление о новом заказе (сейчас
не получает — сетевая проблема с 20.07.2026), провал уведомления виден в
Sentry, а не только через ручной SQL; форма регистрации магазина не молчит
на баге с телефоном; тестовый сьют переживает повторный прогон без
пересоздания БД. Состав согласован в диалоге 21.07.2026 (сразу после
закрытия Спринта 23) — критический путь v1 по-прежнему заблокирован внешне
(ЮKassa-мерчант, юрпакет, ИП), это снова работа вокруг блокера.

Задачи (порядок — сначала сетевой баг, он бьёт по уже заявленной ценности
продукта в проде, остальное — по UX/техдолгу):

- [ ] Бэк: исходящие запросы grammY к Telegram Bot API — кастомный
  `fetch`/agent с ретраем по нескольким известным IP `api.telegram.org`.
  Диагностика 20-21.07.2026 (`backlog.md`): блокировка не всей подсети/домена,
  а конкретного IP (`149.154.166.110`), другие адреса того же диапазона
  отвечают. Перепроверить актуальные рабочие адреса перед реализацией —
  могли смениться (DNS round-robin на стороне Telegram).
- [ ] Бэк: `registerOrderNotificationWorker` — обернуть в try/catch с
  `Sentry.captureException` (по образцу `enqueueOrderNotification`).
  Сейчас провал уходит молча в `failed`-джобу pg-boss, виден только через
  ручной SQL по `pgboss.job`.
- [ ] Фронт: `requestContactPhone()` (`apps/web/src/lib/telegram.ts`) —
  разобраться, что реально приходит от `WebApp.requestContact` на живом
  клиенте (официальная документация Telegram описывает колбэк как
  принимающий только булево `sent`, без данных контакта — код читает
  несуществующий, похоже, `response.responseUnsafe.contact.phone_number`;
  альтернативный канал — событие `contactRequested`). Поправить под
  реальный формат.
- [ ] Фронт: `RegisterForm.tsx` — предзаполнить `+7` в поле телефона по
  умолчанию (тот же паттерн, что в чекауте покупателя, Спринт 21).
- [ ] Тесты: `products-import.route.test.ts` — устойчивость к повторным
  прогонам без пересоздания тестовой БД (`seedSeller` использует общий
  счётчик `tgCounter` от `700460000` без явной очистки между прогонами,
  ловит `duplicate key` на `sellers_telegram_id_unique`).
