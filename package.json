{
  "name": "grammashop",
  "private": true,
  "engines": {
    "node": ">=22"
  },
  "packageManager": "pnpm@11.13.1",
  "scripts": {
    "dev:web": "pnpm --filter @grammashop/web dev",
    "dev:api": "pnpm --filter @grammashop/api dev",
    "build": "pnpm -r build",
    "test": "pnpm -r test",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.7.0"
  }
}
